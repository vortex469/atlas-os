# v0.59 P4 — retained Mission Control evidence

The [normative v0.59 freeze](v0.59-runtime-interface-boundary.md) retains
Core-owned v0.57 interface-prerequisite evidence hardened through v0.58.
Both prerequisites exist at checkout baseline `970ba1bb`: the retained Core/API
surface (including P3 owner-scoped no-store responses) and the repository-integrated
[v0.58 Mission Control view](v0.58-p4-validation.md). Definition-review remains
deferred; this task does not introduce a successor wire contract.

The existing inventory view beneath the exact v0.56 review now explains the
v0.59 disposition in collapsed advanced evidence. Worker start and execution
remain visibly blocked. Core remains responsible for canonical fingerprints,
persistence and trusted-time eligibility; the existing credentialed collection
and exact item GET reader is reused without additional client state or actions.

Focused API regressions reject both true and false unselected admission and
definition-review markers, plus successor schemas, in collection, listed record,
result, record, status and nested review record/status envelopes. Rejected
collections stop before item reads. UI regressions assert unavailable evidence,
blocked state and absent advanced evidence for invented successors. Existing
coverage retains Core golden evidence, exact parent nesting, expiry, scope
changes, late responses, redaction and all fixed-false authority checks.
Structural coverage preserves exactly five production consumers and excludes
successor authority/routes, write controls, polling, persistence and navigation.

## Validation

Run from `services/mission-control` in this managed worktree after `npm ci`:

```bash
npm test -- src/api/workerActivationRuntimeInterfacePrerequisite.test.ts src/features/installation/WorkerActivationRuntimeInterfacePrerequisite.test.tsx src/security/workerActivationRuntimeInterfacePrerequisiteStructural.test.ts src/features/installation/WorkerActivationRuntimePlanReview.test.tsx
npm run build
npm run lint
```

Focused tests passed: **310 tests across four files**, exit 0, 17.44 seconds.
Build passed, exit 0, with the bundle-size warning. Lint passed, exit 0, with
one existing `react-hooks/exhaustive-deps` warning in `WorkflowShellPage.tsx:87`.
No dependency manifests or lockfiles changed.

Hostile review passed: reviewed the complete implementation and test diff after
validation. The only production change is explanatory text matching the freeze
inside already collapsed details. Strict closed-envelope parsing remains in
force for true and false invented markers, including nested predecessor evidence.
Tests preserve valid Core evidence acceptance and fail closed without disclosing
invalid evidence or fetching successor routes. Scope isolation, exact lineage,
Core-derived lifecycle and blocked authority remain intact. No new production
consumer or state exists. `git diff --check` passed; no unresolved findings.
These focused checks do not close historical full-suite, browser E2E or release gates.

## Commit artifact

The task-owned real commit is stored in `.task-evidence/v059-p4/commit.git`,
with the checkout baseline as parent, and exported as an incremental
`.task-evidence/v059-p4/implementation.bundle`. Git writes stay within the
managed worktree because the original Git metadata is outside the permitted
write area. The managed branch is not advanced; the implementation diff remains
available in the worktree.
