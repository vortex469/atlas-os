# v0.60 P0 — Provisional runtime definition evidence boundary

Status: **Provisional architecture/planning only. No normative selection,
implementation authorization, predecessor release closure or new authority.**
Inspected repository baseline: `d66bb00a74fc7aef56928217fd41ec4857590a38`.
Reinspection baseline: `00d586acf2962a90caf500268e72580cb094a11c`.
The existing P0 is retained and refined here; reinspection confirms the same
v0.59 retained-boundary disposition, not a new predecessor acceptance decision.
This document is the sole file owned by this card. It can coexist with parallel
predecessor planning; shared roadmap/release files and production behavior are
outside its scope. Version order alone cannot authorize a successor.

## Observed authority chain

| Inspected source | Consequence for v0.60 |
| --- | --- |
| [v0.55 plan](worker-activation-runtime-plan-v1.md), [v0.56 review](worker-activation-runtime-plan-review-v1.md) | Reference-only design and consistency review preserve unresolved runtime interfaces; neither defines a runnable installation runtime. |
| [v0.57 Core contract](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/contract.py) | The accepted evidence is a closed, fixed seven-entry blocker/owner/proof inventory over exact v0.56 review lineage. Inventory correspondence already has deterministic validation. |
| [v0.58 freeze](v0.58-runtime-interface-admission-provisional.md) | Retains v0.57 and defers redundant inventory admission. There is no v0.58 definition pair. |
| [v0.59 normative boundary](v0.59-runtime-interface-boundary.md), [P5 evidence](v0.59-p5-validation.md) | Retains the same wire boundary with hardening; definition-review P1-P5 are deferred. Repository integration is observed, but predecessor and v0.59 release closure remain open pending required Mission Control gates. Historical dependency failures are not a fresh environment diagnosis by this card. |
| [Owned reader](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/readers.py), [service](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/service.py), [store](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/store.py) | Core owns validation, trusted time and durable evidence. The reader consumes v0.56; it is not a successor reader of v0.57. Item GET derives current status; list returns records without paired statuses. |
| [Retained closure](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py), [v0.59 closure](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v059_release_closure.py) | Exact twelve-file production consumers and rejection of unselected successor authority remain enforced; no future package is exempted by this plan. |
| [Agent architecture](../../services/atlas-agent/ARCHITECTURE.md), [execution worker](../../services/atlas-execution-worker/README.md) | Repository execution and operational dispatch have separate authentication, requests, approvals and ledgers. Packaged worker health or backend activation cannot supply installation runtime authority. |

The actual direct predecessor candidate is
`WorkerActivationRuntimeInterfacePrerequisiteV1` paired with
`WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, both v0.57 schemas retained
through v0.59. The chain remains v0.57 inventory -> v0.56 review -> v0.55 plan ->
v0.54 runtime admission -> v0.53 runtime prerequisite -> v0.52 queue receipt ->
v0.51 acknowledgement admission -> v0.50 acknowledgement prerequisite -> v0.49
claim admission -> every inherited v0.48-v0.20 pair in the v0.55 contract.
There is no v0.58/v0.59 evidence wrapper, successor reader, ID or hash domain.
The v0.52 receipt contains injected already-observed assertions; its lineage
does not attest that Core contacted or independently verified a queue.

## Likely next narrow boundary

**Conditional candidate: Core-owned, reference-only installation runtime
definition evidence for the first unresolved proof obligation.** It would specify
abstract runtime identity, capability composition and lifecycle constraints for
one exact owned inventory pair. It would not implement the runtime or define
all seven interfaces at once. Names, wire schemas, markers, domains and protocol
vocabulary remain unselected; this P0 reserves none of them.

The intended new information is a bounded declarative relationship between an
abstract runtime subject, its allowed capability composition and lifecycle
states, including explicit incompatible combinations and terminal uncertainty.
The inventory currently names that proof obligation without supplying those
relationships. A future normative decision must establish the actual closed
vocabulary, deterministic source of each declaration and a concrete consumer
need. Caller prose, model suggestions, endpoints, credentials, command bytes,
executable configuration and worker discovery cannot supply trusted definitions.
An abstract identity is not an authenticated live worker identity.

This candidate remains a hypothesis: the inspected repository does not establish
that a separate persisted definition is useful or feasible. Before selection,
show an accepted definition example and a conflicting capability/lifecycle
example that the retained inventory alone cannot distinguish. Specify whether
the latter is invalid input or a closed negative finding; do not silently repair
it. Complete structural declarations with missing operational proof must still
leave every blocker unresolved. If the examples merely repeat v0.55 design,
v0.56 review or v0.57 correspondence, retain documentation only.

| Integrated predecessor confirmation | Required v0.60 disposition |
| --- | --- |
| v0.59 retains the inventory, as observed here | Evaluate the narrow definition candidate against distinctness, provenance and feasibility gates; no automatic P1 start. |
| Later accepted v0.59 supplies an actual definition or already validates these relationships | Re-analyze against that exact contract; do not duplicate it or automatically revive the deferred definition review. |
| Predecessor contract/lineage is missing, incompatible or not accepted for successor work | Keep P1-P5 blocked; no synthetic pair, older-stage fallback or inferred release acceptance. |
| No distinct predicate/consumer or bounded evidence cannot fit | Keep v0.60 documentation only; another version number is not evidence progress. |
| Proposed work requires contact, activation or effects | Require separate normative authority design; this candidate cannot expand into an adapter or start protocol. |

## Candidate comparison and normative handoff

The first inventory entry is a useful scoping anchor, not an established ordering
of implementation dependencies. Its Core owner and declarative proof obligation
make it the likely narrowest candidate that does not require a worker contact
boundary. Final v0.59 confirmation must still justify choosing it first.

| Candidate | Provisional disposition and reason |
| --- | --- |
| Another inventory admission or correspondence review | Defer: the retained closed inventory already validates those relationships; a new marker would add no demonstrated predicate. |
| Abstract runtime identity, capability composition and lifecycle definition | Investigate only: potentially adds declarative relationships absent from the fixed inventory, within Core installation evidence ownership. Trusted declaration provenance and consumer need are still unproved. |
| Store/runtime contact definition or adapter | Defer: separate worker storage/runtime owners and unresolved authentication, protocol, effects and recovery obligations cannot be inferred from a Core journal. |
| Worker start admission, start or Agent/execution integration | Blocked: the inventory supplies neither operational runtime/contact proof nor exact downstream approvals and durable effect authority. |

Before P1, a separately scoped normative decision should contain the following
reviewable evidence. These are handoff requirements, not results supplied here:

- Pin the accepted integration commit and exact predecessor record/status
  contract, with links to predecessor release evidence and explicit resolution of
  the open v0.58/v0.59 gates. A source commit or passing P0 scan alone is insufficient.
- Compare two otherwise identical exact inventory inputs with proposed runtime
  declarations: one internally consistent, one with a capability combination
  disallowed by its declared lifecycle constraints. Explain the deterministic
  distinction, the trusted source of the constraint and the consumer decision it
  supports. These are conceptual cases, not selected literals or executable data.
- Show that both cases still lack operational proof and cannot clear any of the
  seven blockers. Also show that foreign ownership, substituted predecessor
  fingerprints and expired lineage prevent acceptance regardless of declaration
  consistency. Record the chosen refusal/negative-finding semantics explicitly.
- Supply measured full expanded record, status, result and collection sizes,
  plus evaluation timing for the exact predecessor and proposed embedding.
  Include an over-bound case that refuses without truncation or limit changes;
  no successor feasibility measurement is claimed by this document.
- Reconcile the exact new and shared paths with their phase owners and existing
  consumer-isolation tests. Identify the separately authorized test updates a
  selected successor would require; this P0 grants no scanner exception.

If any item remains unproved, retain this provisional document and keep P1-P5
deferred. Closing the predecessor release gates enables reconsideration; it does
not itself select this candidate or authorize implementation.

## Assumptions requiring integrated v0.59 confirmation

All items below are open handoff gates, not facts established by this P0.
Existing source integration does not establish final acceptance of concurrent
changes. A later normative decision must record the actual decision and evidence.

1. **Accepted predecessor:** pin the final v0.59 integration commit, normative
   contract, retained v0.58 requirements, P1-P5 evidence and outstanding external
   gates. Resolve open predecessor and v0.59 release closure before successor
   implementation. This P0 does not rerun or waive the full UI tests/build/lint.
2. **Distinctness and provenance:** demonstrate new capability/lifecycle
   relationships and a justified consumer; freeze who authors the trusted closed
   definition and how Core validates it. The first inventory owner is
   `core_installation_authority`; its proof is
   `installation_runtime_identity_capability_composition_lifecycle`.
3. **Exact owned pair:** confirm the actual predecessor schemas and read
   semantics. Under the retained outcome, a new reader must bind authenticated
   `operator_id`, `candidate_record_id`, `runtime_interface_prerequisite_id`,
   record fingerprint, stable status fingerprint and exact expiry. Derive stable
   status at inventory `recorded_at`, with independent eligibility checks at
   trusted whole-second UTC Core time. Arbitrary item-GET status is not a stable
   pin; list results are not record/status pairs.
4. **Recursive integrity:** preserve complete historical evidence, IDs, subjects,
   times, authority and version-specific hashes. Reparse copied/constructed
   models; reject extra/duplicate fields, coercion and wrong hashes. Preserve all
   six predecessor IDs; v0.52 shares v0.51 `admission_id`, while v0.54 has its own
   `runtime_admission_id`. Never invent a receipt ID, normalize lineage or accept
   caller-supplied nested evidence, foreign scope, latest fallback or worker lookup.
5. **Bounded feasibility:** measure real predecessor plus proposed successor
   model/response size and evaluation duration. Retained ceilings are 192 KiB per
   full model/response, 30-second freshness, 16 reservations per owner, 256 globally
   and 256 MiB main SQLite pages (not a filesystem quota). Preserve inherited
   exact expiry; reject future, stale, expired, corrupt or ambiguous evidence.
   No freshness renewal, recursive truncation or larger limits to fit the stage.
6. **Durability and identity:** freeze separate successor hash/UUID domains and
   independent vectors only after selection. Bind the permanent subject to the
   owner/candidate/exact predecessor, independently of key, content and expiry.
   Require locked revalidation, reservation before append, terminal uncertainty
   and restart no-replay without mutating predecessor journals.

## Authoritative subsystem and blocked downstream authority

Core installation evidence would own definition evaluation, canonical identity,
eligibility and persistence. The crossing from caller to Core admits only
authenticated scope and validated exact references under the future closed
contract; caller claims cannot confer approval. Owned predecessor provenance
does not prove operational compatibility. Journal persistence records evidence,
never readiness. Mission Control remains a non-authoritative read projection;
cached UI success and browser time cannot grant eligibility. Agent, relay and
execution worker receive no new consumer or authority.

All seven retained blockers remain, even if a future definition is recorded:

| Blocker | Authority that still needs its own accepted proof/boundary |
| --- | --- |
| `worker_activation_runtime_not_defined` | Core installation runtime identity, capability composition and lifecycle proof; a declarative record alone does not discharge it. |
| `store_contact_not_defined` | Worker storage authentication, exact subject, protocol operations and recovery. Core's evidence journal is not this interface. |
| `runtime_contact_not_defined` | Worker runtime authenticated peer/request, effect limits and uncertainty handling. |
| `worker_start_admission_not_defined` | Core admission over validated runtime/contact prerequisites and exact subject. |
| `worker_start_not_defined` | Worker-owned one-shot start reservation, durability and no-replay. |
| `agent_invocation_not_defined` | Agent installation intent, authentication and exact approvals. |
| `execution_start_boundary_not_defined` | Execution request, capability checks, approval and durable ledger. |

Preserve `evidence_only=true`, `reference_only=true`, `payload_bytes=0`, all 67
fixed-false authority/material fields and historical true markers with their
original meanings. No queue claim/lease/ack/consume/requeue, store/runtime contact,
activation, start admission/build/start, Agent invocation, execution authorization,
shell/process/container, scheduling, provider/repository/guest mutation,
installation, deployment, rollback, publication, retry/resend or repair/replay
bypass follows. Home Assistant remains blocked without installation artifacts.

## Expected file ownership and P1-P5 responsibilities

Only this file may change for P0. The paths below are conditional ownership
proposals, not reserved production consumers or implementation authorization.
Normative planning must reconcile names and exact shared-file ownership first.
No roadmap, release checklist, changelog, tag, dependency, compose or deployment
edit belongs to this card. Historical closure scanners must not be weakened to
make a hypothetical consumer pass.

| Phase | Conditional ownership and exit evidence |
| --- | --- |
| P1 | Proposed new `services/atlas-core/app/worker_activation_runtime_definition/contract.py`, vectors and adjacent contract tests. Freeze immutable closed declarative models, distinct acceptance/refusal semantics, recursive authority and independent identity vectors; test incompatible compositions, unresolved proof, forged lineage and size/time limits. Pure evaluation, no I/O. |
| P2 | Proposed same package `readers.py`, `service.py`, `store.py` and durability tests. Build an exact owned v0.57 reader only if the retained predecessor is confirmed; stable historical status plus trusted current eligibility, default-off explicit construction, separate bounded journal, permanent reservation and no-replay. Prove byte-exact lineage, unchanged predecessor database, owner/readback isolation, corruption and restart refusal. |
| P3 | Proposed `services/atlas-core/app/routes/worker_activation_runtime_definition.py` and route tests; separately coordinate `app/api/v1/router.py` and `app/operator_auth/models.py` under `services/atlas-core/`. Freeze only guarded evidence create/list/get if justified: authentication, Origin/CSRF, exact idempotency, strict bounded framing, owner-bound responses, no-store and redacted errors. No runtime bridge or production service construction. |
| P4 | Proposed definition API/types/hook/component under `services/mission-control/src/`, with exact parent `components/WorkerActivationRuntimeInterfacePrerequisite.tsx` integration reconciled at selection. GET-only nested evidence, strict envelopes and subject binding, stale-request cancellation and cleared obsolete state; all blockers visible. No start/write controls, polling, persistence or standalone navigation. Test malformed and foreign data and unchanged authority projection. |
| P5 | Proposed new package closure/isolation tests and version-specific validation evidence. Prove the accepted durable chain, fixed authority, exact approved consumers, zero Agent/worker consumers, unchanged historical isolation and required Core/API/UI tests/build/lint. Separate code integration from predecessor acceptance and release closure; report external failures honestly. Shared release synchronization requires its own scoped task. |

No conditional phase is completed by this document. If normative selection fails,
all five remain deferred and no package, marker, route or UI consumer is created.

## P0 validation scope

Validate local document links and whitespace, and run retained v0.59 normative
inventory/consumer checks plus v0.57 exact-consumer/authority-ceiling checks.
Review the final diff for sole-file ownership, unsupported predecessor claims,
duplicate evidence stages, accidental authority promotion and conditional phase
language. These checks establish planning consistency only; they do not prove
successor feasibility, UI release closure, runtime readiness or deployment.

### Reinspection validation (2026-09-14)

Focused tests passed: **5 passed, 132 deselected, 326 warnings in 1.60s** using
the selected Python interpreter's `-m pytest` from the managed worktree with
`PYTHONPATH=services/atlas-core`. Selected checks were
`test_v059_normative_inventory_matches_retained_core`,
`test_v059_no_successor_or_workflow_consumer`,
`test_exact_v057_production_consumers`,
`test_normative_inventory_authority_and_downstream_ceiling` and
`test_normative_false_inventory_is_exact`, in the retained prerequisite package's
`test_v059_release_closure.py`, `test_release_closure.py` and `test_contract.py`.
An initial invocation failed at setup because the local basetemp parent was
missing; creating that worktree-local directory resolved all five setup errors.
All 13 local document links resolve inside the worktree; `git diff --check` passed.

Hostile review passed: the sole-document diff preserves provisional selection,
exact retained predecessor identity, all seven blockers, conditional file
ownership and deferred P1-P5. Candidate ordering is explicitly an assumption;
conceptual distinguishing cases are not trusted definitions, selected vocabulary
or measured feasibility. No production code, consumer allowance or shared
roadmap/release file changes. Full UI release checks remain predecessor handoff
requirements and are not claimed by these focused planning checks.
