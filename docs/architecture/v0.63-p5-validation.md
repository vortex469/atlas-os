# v0.63 P5 — retained authority isolation and release evidence

The [normative v0.63 boundary](v0.63-authority-boundary.md) retains the
Core-owned v0.57 interface-prerequisite pair with the integrated v0.62 state.
P5 closes the v0.63 retention boundary only: successor runtime-definition
authority remains deferred, and predecessor release acceptance remains open.

## Closure regressions

`test_v063_release_closure.py` adds 43 cases. It rejects inventory-admission,
runtime-definition and definition-review marker forms across every shared
Core/UI evidence envelope, including both true and false values. It also
forbids successor and operational consumers across Core, Agent, execution
worker, Mission Control, configuration, deployment, scripts and workflows.
Test sources and golden fixtures remain evidence and are excluded from the
production scan.

The retained v0.63 suites continue to prove the exact schemas, complete
lineage, bounds, bounded journal restart/no-replay behavior, guarded API
default-off behavior and nested GET-only Mission Control projection.

## Reproducible focused validation

Run from the managed worktree with the selected interpreter:

```bash
mkdir -p .task-evidence/v063-p5
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v063_release_closure.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v063_retained_boundary.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v063_durable_evidence.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v063_guarded_api.py \
  --basetemp=.task-evidence/v063-p5/pytest --tb=short --disable-warnings
```

Focused tests passed: **66 passed**, exit 0. `git diff --check` passed.

## Release disposition and hostile review

V0.63 release closure remains open. Repository-integrated predecessor
acceptance and the independent full Core/worker release obligations remain
required. This evidence is not a release, tag, publication, deployment or
runtime enablement.

Hostile review passed: the closure suite rejects false-valued successor
markers, checks nested shared envelopes, excludes only test evidence from the
production scan, and preserves the exact retained pair and no-authority
ceiling. No production runtime, dependency, deployment or consumer is added.
