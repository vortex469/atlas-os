# v0.58 P5 retained-boundary release evidence — closure open

Implementation baseline: `f3609db51938b8df7343bcb0ca38d97f2d17f06e`.
The [normative contract](v0.58-runtime-interface-admission-provisional.md) retains
v0.57 interface-prerequisite evidence. A2 still defers a redundant admission
stage. This P5 adds repository regressions and synchronization documentation;
it changes no production code, historical isolation exception, consumer,
authority, deployment file or dependency lockfile.

## Integration and proof coverage

Ancestry checks passed for v0.57 Sync `0ea05947`, P1 `f4c1632f`, P2 `8d160eec`,
P3 `78702530`, P4 `89759163`, P5 `6a35f569`; and v0.58 P0 `451c34e2`,
Sync `054a5790`, retained P1 `973aa2a8`, P2 `f2def85b`, P3 `a71f0216`,
P4 `f3609db5`. The full resolved identities are in the local ancestry log.

| Requirement | Repository regression evidence |
| --- | --- |
| Exact v0.57 lineage and ownership | Retained contract vectors and recursive hostile lineage tests; durable v0.56 reader in `test_release_closure.py`; scoped service/API tests. Six IDs, original expiry, complete immutable review/status and unchanged predecessor journal bytes. |
| Replay and restart durability | Closure reopens the actual journals, expires evidence, preserves exact duplicates without predecessor reads and refuses a new key for the permanent subject. Service/store tests cover independent processes, both write locks, incomplete reservations, lost acknowledgements and terminal audits. |
| Bounded persistence and corruption closure | Full service/store tests cover lowered capacity, serialized/page bounds, no eviction, corrupt connections/restart, missing live storage without recreation, CWD/symlink redirection and SQLite URI metacharacters. |
| API/UI isolation and non-authority | Full API suite covers authentication, owner/candidate/item/request binding, strict/redacted bounded responses and hostile framing before writes. Historical API inventory locks exact routes. Current UI test execution remains blocked as recorded below. |
| Zero unauthorized consumers and blocked effects | Existing exact twelve-file production inventory scans Core, Agent, worker, UI and deployment. New P5 scan also rejects every deferred-admission marker in production trees and workflows with no allowed successor path. Historical blocked Home Assistant goldens remain required and pass in the historical selection. |
| Synchronization closure | New P5 checks bind all seven normative inventory rows and retained ceilings to Core. The shared complete UI golden is positively validated by Core, then invented admission authority is rejected in all five envelopes for both true and false values. |

No new reader consumes v0.57 for a successor. V0.58 adds no worker/runtime
interface, contact, start, Agent invocation, execution or effect authority. Mission
Control remains a nested credentialed GET-only evidence reader. The documentation
records integrated P1-P4 hardening without turning historical markers into
current readiness or admission. Admission deferral is not a completed admission
implementation.

## Commands and observed results

Commands run from the managed worktree using the selected Python interpreter.
All logs and pytest temporary directories are worktree-local under
`.task-evidence/v058-p5/`; these ignored local logs are not portable release
artifacts. This committed document records the observed results.

```bash
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite \
  services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py \
  --basetemp=.task-evidence/v058-p5/core-tmp --tb=short --disable-warnings

PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v058_release_closure.py \
  --basetemp=.task-evidence/v058-p5/closure-tmp --tb=short --disable-warnings

PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app \
  -k 'release_closure or mission_control_isolation or test_installation_release_isolation' \
  --ignore=services/atlas-core/app/worker_activation_runtime_interface_prerequisite \
  --basetemp=.task-evidence/v058-p5/history-tmp --tb=short --disable-warnings
```

- New P5 focused regressions: **12 passed, 326 warnings in 27.58s**, exit 0
  (`closure.log`). Added after the full Core run collected tests, so run separately.
- Historical release closure, Mission Control isolation and installation release
  isolation: **156 passed, 4273 deselected, 407 warnings in 151.87s**, exit 0
  (`history.log`). The retained package is excluded only from this invocation
  because it has its own full run; no historical scanner or test is modified.
- Full retained package/API: **221 passed, 453 warnings in 2629.23s
  (43:49)**, exit 0 (`core.log`). This is a completed invocation of all retained
  contract, durable closure, service/store and guarded API tests. Together with
  the separate 12-test P5 run, all 233 current retained package/API cases passed.
  The synchronized-contract regression was also rerun after documentation edits:
  **1 passed, 11 deselected in 1.22s**, exit 0 (`docs-test.log`).
- Core Ruff release gate: **576 changed Python files passed**, exit 0
  (`ruff-gate.log`). Used the exact RC1 baseline
  `0216b7bfe7f3b160a762269802aa34244ae70a72` and changed/untracked Python file
  selection from `scripts/rc1-python-ruff-gate`, invoking the selected interpreter
  with `-m ruff check`. The new test also passes Ruff formatting. An additional
  whole-tree `-m ruff check services/atlas-core/app` reports **84 existing
  findings** (`ruff.log`); a clean whole-tree Ruff result is not claimed.

```bash
PYTHONPATH=services/atlas-agent /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-agent/tests/test_delivery_activation_preflight_isolation.py \
  services/atlas-agent/tests/test_v036_installation_execution_admission_isolation.py \
  services/atlas-agent/tests/test_v037_runner_binding_plan_isolation.py \
  --basetemp=.task-evidence/v058-p5/agent-tmp

PYTHONPATH=services/atlas-agent:services/atlas-execution-worker \
  /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-execution-worker/tests/test_config.py \
  services/atlas-execution-worker/tests/test_healthcheck.py \
  services/atlas-execution-worker/tests/test_durable_ledger.py -k 'not api' \
  --basetemp=.task-evidence/v058-p5/worker-confirm-tmp
```

Agent isolation: **6 passed in 0.06s**, exit 0 (`agent.log`). Worker confirmation:
**24 passed, 1 deselected, 1 warning in 0.47s**, exit 0 (`worker-confirm.log`).
The first identical worker selection used `worker-tmp` and failed:
**23 passed, 1 failed**, SQLite `database is locked` during concurrent WAL
initialization in `test_concurrent_instances_have_one_claim` (`worker.log`).
The confirmation is not proof that this existing race is fixed. No worker runtime
change or replay/retry behavior is introduced. The threaded API test is excluded
as in the historical isolation gate and is not claimed passed.

## Required Mission Control gates — blocked

```bash
npm ci --prefix services/mission-control \
  --cache "$PWD/.task-evidence/npm-cache" --no-audit --no-fund
npm ci --offline --prefix services/mission-control \
  --cache "$PWD/.task-evidence/npm-cache" --no-audit --no-fund
npm test --prefix services/mission-control -- --maxWorkers=2 --cache=false
npm run build --prefix services/mission-control
npm run lint --prefix services/mission-control
```

Environment error: Node 22.23.1/npm 10.9.8 are present, but registry DNS returns
`EAI_AGAIN`. Online installation exits 1 with npm's `Exit handler never called`;
offline installation exits 1 with `ENOTCACHED`. Test exits 127 (`vitest: not
found`); build exits 1 (missing Vite/Node types, incompatible fallback TypeScript
rejecting `erasableSyntaxOnly`); lint exits 127 (`eslint: not found`). Logs are
`npm-ci.log`, `npm-offline.log`, `ui-test.log`, `ui-build.log`, `ui-lint.log`.
Dependencies, manifests and lockfile were not modified in Git.

[P4 evidence](v0.58-p4-validation.md) records historical focused UI test/build/lint
success. It does not replace these required current P5/full-suite gates. Restore
the locked dependencies and rerun the full suite, build and lint before closure.
V0.57/v0.58 repository release closure therefore remains **open**; no release tag,
publication, deployment or production construction is claimed.

## Diff review and commit disposition

Focused tests passed: 221 retained Core/API cases and 12 new P5 cases, plus the
156 historical regressions and the separately recorded isolation checks above.
This is not a full repository or Mission Control validation pass.

Hostile review passed: reviewed the complete six-file test/documentation diff
after the focused checks, including the final Core/API result. Verified positive
Core controls for the shared UI golden and reason-specific extra-field refusals,
true/false successor mutations in all five envelopes, fixed inventory/bounds,
zero successor allowances and exact retained consumer ownership. No production,
historical test, authority, dependency or deployment file changed. Clarified that
UI test execution is blocked and that no-new-authority claims refer to v0.58.
No further defect or missing regression was found in this diff; unavailable UI
validation remains an explicit release blocker, and the worker race is not
claimed fixed.

Documentation checks passed for 28 local link targets, twelve phase ancestors,
exact six-file scope and byte-identical historical checklist/changelog evidence.
Ruff formatting, `git diff --check` and `git diff --cached --check` passed. Only
worktree-local temporary paths were used. No full repository suite, browser E2E,
release acceptance or runtime enablement is claimed.

This evidence is included in the local commit titled
`test: add v0.58 retained-boundary release closure evidence`, parented to the
implementation baseline above. Staging and commit use the managed worktree's
normal Git metadata; no alternate Git directory or metadata relocation is used.
Repository release closure remains open until the required UI gates pass.
