# v0.63 Sync — retained authority boundary

Status: **Normative repository planning freeze. Retain the Core-owned v0.57
prerequisite record/status pair with repository-integrated v0.62 evidence.
Successor P1-P5 remain deferred. No successor implementation is authorized.**

## Integrated baseline and decision

Inspected on 2026-09-20 at `426cac84c7d4a287684752f38e8284e01d65dea2`, which is
the repository-integrated v0.62 state and `atlas-v0.62.0` tag. The v0.63 P0
at `24dfdeebdad77341b496cf9256001d01c25e772b` is an ancestor. The v0.62 P5
evidence records retained-boundary closure and the execution-worker durable
ledger fix records operational reliability hardening. Neither establishes
release acceptance or worker activation authority.

This Sync supersedes the provisional P0 selection and conditional phase
proposals with documentation-only retention. The [v0.62 normative boundary](v0.62-authority-boundary.md)
remains binding in full: exact schemas, identities, fingerprint and UUID
domains, recursive lineage, time, durability, API, ordered inventory,
authority ceiling and consumer ownership are unchanged. The v0.63 label
creates no evidence stage, migration, schema, marker, domain, reader or route.

| P0 candidate | Frozen disposition |
| --- | --- |
| Inventory admission or another consistency review | Deferred: correspondence is already checked and no distinct consumer decision is demonstrated. |
| Runtime identity/capability composition/lifecycle definition | Deferred: trusted provenance, distinct usefulness and bounded embedding remain unproved. |
| Runtime definition review | Deferred: no accepted definition pair exists to review. |
| Store/runtime contact or adapter | Requires a separate authority design for authenticated subjects, operations, effects and recovery. |
| Worker start, Agent invocation or execution integration | Blocked by unresolved runtime/contact proofs and missing exact approval and durable-effect boundaries. |

## Retained output and authority ceiling

The accepted pair remains `WorkerActivationRuntimeInterfacePrerequisiteV1` /
`WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, with schemas
`worker-activation-runtime-interface-prerequisite-v1` and
`worker-activation-runtime-interface-prerequisite-status-v1`. It retains the
complete v0.56 review and inherited v0.55-v0.20 lineage. All six
predecessor/stage UUID fields, owner/candidate binding, version-specific
hashes, 192 KiB model/response bound, 30-second freshness, 16 per-owner
reservations, 256 global reservations and 256 MiB main SQLite page bound
remain exact.

The integrated v0.62 delta is retention and release-isolation hardening only:
immutable contract and shared-envelope refusal, bounded durable journal and
no-replay behavior, guarded API and nested non-authoritative Mission Control
evidence, exact consumer isolation, and execution-worker ledger reliability.
The ledger is an adjacent operational record, not Core installation-runtime
evidence, and adds no successor pair, reader, route, consumer or authority
field.

All seven ordered blockers remain unresolved. Preserve `evidence_only=true`,
`reference_only=true`, `payload_bytes=0`, all 67 fixed-false authority/material
fields and the original meanings of historical true evidence markers. No
queue claim/lease/ack/consume/requeue, contact, activation, start, Agent
invocation, execution, installation, deployment, rollback, publication,
retry/resend or repair/replay authority follows. Mission Control remains a
nested, non-authoritative GET-only projection; Agent, worker and operational
consumers remain zero.

## Phase disposition and reopening

| Phase | Frozen v0.63 disposition |
| --- | --- |
| P1 | Successor contract/evaluator/vectors deferred; retain the existing Core contract. |
| P2 | Successor reader/service/journal deferred; retain the existing owned reader and journal. |
| P3 | Successor API deferred; retain the existing guarded prerequisite API. |
| P4 | Successor presentation deferred; retain nested GET-only evidence. |
| P5 | Successor release closure deferred; retained v0.62 isolation evidence is recorded in [v0.62 P5](v0.62-p5-validation.md), while predecessor acceptance remains independent. |

Reopening requires a separate normative decision satisfying all six gates in
[v0.63 P0](v0.63-provisional-authority-boundary.md#evidence-required-before-a-successor-can-be-selected):
accepted predecessor, distinct trusted predicate, exact owned pins, recursive
integrity and measured bounds, durable uncertainty/no-replay, and hostile
no-authority-promotion evidence. If any gate remains unproved, retention
continues. Release acceptance alone cannot select a successor.

## Release and synchronization scope

Repository-integrated v0.62 and inherited release acceptance remain open where
recorded in the [release checklist](../RELEASE_CHECKLIST.md). The v0.62
closure and execution-worker durability evidence are isolation/reliability
evidence, not permission to close those obligations. Full Core/worker gates
remain separate and unwaived. Sync owns this document, its [validation record](v0.63-sync-validation.md),
the P0 supersession notice, `ROADMAP.md` and `docs/RELEASE_CHECKLIST.md`.
Production code, tests, dependencies, scanner allowances and deployment remain
outside this documentation freeze. No release, tag, publication or runtime
enablement follows.
