# v0.60 P4 — retained Mission Control evidence integration

The [v0.60 synchronized contract](v0.60-runtime-definition-boundary.md) retains
the Core-owned v0.57 interface prerequisite pair integrated through v0.59.
The [P3 API hardening](v0.60-p3-validation.md) adds no successor endpoint or
runtime-definition evidence. Mission Control's existing exact-review nested
reader already consumes that retained API. This change updates its advanced
explanation to the v0.60 disposition: runtime definition remains deferred and
no new runtime authority is established.

Focused regressions reject invented runtime-definition markers (including false
markers) and a successor result schema across collection, listed record, item
result, record, status and nested review envelopes. Presentation regressions
verify those responses reveal no evidence details. Additional tests verify Core
lifecycle survives browser clocks before and after evidence expiry, and an item
503 following a successful collection read cannot expose listed evidence,
dependency details or retry controls, or trigger a reread on equivalent renders.
The structural guard also forbids runtime-definition routes and markers.

No schema, Core state, persistence, clock-derived eligibility, fingerprint
calculation, navigation, write control or production consumer is added. Evidence
remains a credentialed collection/item GET projection beneath the exact review,
with collapsed details and blocked worker start and execution.

## Validation

Run from `services/mission-control` in the managed worktree after `npm ci`:

```sh
npm test -- src/api/workerActivationRuntimeInterfacePrerequisite.test.ts src/features/installation/WorkerActivationRuntimeInterfacePrerequisite.test.tsx src/security/workerActivationRuntimeInterfacePrerequisiteStructural.test.ts
npm run build
npm run lint
npm test
```

Focused tests passed: 3 files, 321 tests. Build passed with a bundle-size warning.
Lint passed with zero errors and one existing hook-dependency warning in
`WorkflowShellPage.tsx`.
Full Mission Control tests passed: 164 files, 2,077 tests, exit 0. The local
full-suite log is `.task-evidence/v060-p4-full-test.log` at the repository root.

Hostile review passed: the production diff only updates the retained boundary
explanation inside existing advanced evidence details. All state still comes
through the existing strict Core reader. Regression coverage includes deferred
authority at every retained envelope, browser clock independence, redacted item
failure, exact-parent nesting, scope-change races, fixed-false authority and
the unchanged five-consumer UI boundary. Fake Date is restored in a finally
block. No dependency or lockfile changes are included. `git diff --check` passed.

This task does not reopen deferred runtime-definition implementation or claim
historical release closure, publication or deployment.

## Commit storage

Shared worktree Git metadata became read-only when staging the validation record.
The task commit is stored in `.task-evidence/v060-p4-commit.git` with the original
worktree HEAD as parent and exactly the five reviewed file changes. A portable
bundle is stored at `.task-evidence/v060-p4.bundle`. The shared branch pointer
is unchanged; integration must import the task commit.
