# v0.58 Sync validation evidence

Baseline: `9f15958e6ef4202eed04b0a184106e553b53c6e1`, 2026-09-12.
Scope: documentation/contract synchronization only. The
[normative freeze](v0.58-runtime-interface-admission-provisional.md) selects the
retained v0.57 Core/API inventory boundary and triggers P0 A2 deferral of the
redundant admission P1-P5. No successor implementation is claimed.

## Reconciliation evidence

- v0.58 / 01 P0 `451c34e2` and v0.57 Sync `0ea05947`, P1 `f4c1632f`,
  P2 `8d160eec`, P3 `78702530`, P4 `89759163`, P5 `6a35f569` are ancestors
  of the inspected baseline. The original P0 no-implementation and commit-blocker
  statements are historical, not current repository facts.
- Integrated Core models, evaluator, frozen independent hash/UUID vectors,
  owner-scoped reader, store/service, guarded API and closure consumer inventory
  are the authority for the freeze. Historical future-tense v0.57 prose does not
  override its implemented vectors or reader.
- The proposed admission predicate (valid exact complete inventory) is already
  enforced by v0.57. No additional repository consumer need is demonstrated.
  A2 requires deferral; A5 successor capacity is unproved, not claimed passed.
- All six identities, canonical domains, owner/candidate binding, stable versus
  current-time status, inherited expiry, permanent reservations, exact seven
  inventory triples, 67 false fields and twelve production consumers are retained.
- V0.57's required UI tests/build/lint remain open as recorded in the historical
  release checklist. This task neither reruns nor waives those gates; no UI or
  production file changes require new UI validation for this documentation diff.

## Focused validation

Run from the managed worktree using the selected interpreter:

```bash
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py \
  services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py \
  --basetemp=.task-evidence/v058-sync/pytest
```

Focused tests passed: **135 passed, 444 warnings in 1350.17s (22:30)**, exit 0.
Local log: `.task-evidence/v058-sync/focused.log`. Warnings include existing
Pydantic schema-shadowing and hostile serialization fixtures. This selection checks the
integrated contract, hashes/IDs, hostile recursive lineage and authority, durable
closure/restart, exact consumer isolation, normative inventory and guarded API.
It does not replace the full persistence suite or the open UI release gates.

## Documentation and hostile review

Documentation checks passed: **23 local links/anchors**, exact seven ordered
inventory triples, six ID fields in record/status, 67 false fields and unchanged
v0.56/v0.57 authority models, seven phase ancestors, documentation-only diff and
`git diff HEAD --check`. Shared-file link checks cover the added planning sections;
historical release evidence is byte-identical. The temporary checker initially
included the table header and then an unrelated historical checklist anchor;
corrected scope checks passed without changing historical documentation.

Hostile review passed: reviewed the complete five-file diff after the 135-test
run. Checked unsupported successor claims, duplicate validation, gate bypass,
reader/status confusion, historical marker versus current eligibility, expiry
renewal, ID/hash domain substitution, lineage truncation, ownership expansion,
false-field coercion and misleading release closure. The selected deferral is
explicit across contract, roadmap and checklist; A1 release limitations and A5
unproved successor feasibility are retained. Exact consumer scans and authority
regressions pass, and no runtime/test/allowlist or protected compose file changes.
No missing implementation test is concealed: admission implementation is deferred.

Commit disposition: staging in the managed worktree succeeded. This evidence is
included in the local documentation commit titled
`docs: freeze v0.58 against integrated v0.57 Core/API boundary`, parented to the
baseline above. No alternate Git directory or metadata relocation is used.
No full repository suite, release tag, publication, deployment or production
construction is claimed. Historical release evidence is retained unchanged.
