# v0.61 Sync validation — integrated v0.60 retention

Inspected baseline: `827c751d403a7537e7d076f327f50b89e8336ba7` (2026-09-14).
The [normative freeze](v0.61-authority-boundary.md) reconciles integrated v0.61
P0 with v0.60 P5 `819e17705e11721ff8c0e13e973b31f9b67c79f7` and retains
the v0.57 pair. Successor P1-P5 remain deferred; no release acceptance follows.

## Focused validation

Run from the managed worktree with the selected Python interpreter's `-m pytest
-q`, `PYTHONPATH=services/atlas-core`, and
`--basetemp=.task-evidence/v061-sync/pytest --tb=short --disable-warnings`.
Create `.task-evidence/v061-sync` first. Selection relative to
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`:

- `test_v060_release_closure.py` (all 15 cases)
- `test_v060_retained_boundary.py::test_v060_freeze_retains_exact_inventory_and_schemas`
- `test_release_closure.py::test_exact_v057_production_consumers`
- `test_release_closure.py::test_normative_inventory_authority_and_downstream_ceiling`
- `test_contract.py::test_normative_false_inventory_is_exact`
- `test_contract.py::test_frozen_versioned_hash_and_uuid_vectors`

Also select these from
`services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py`:

- `test_permissions_openapi_method_and_authority_isolation`
- `test_exact_registered_surface_and_zero_agent_worker_consumers`
- `test_production_startup_does_not_construct_v057_review_service`

These check recursive true/false successor-field refusal, no definition or
operational consumers, the exact twelve production paths, retained schemas and
inventory, 67 false fields, independent hash/UUID vectors, exact guarded API
methods/permissions and absent production service construction. The normative
v0.61 document incorporates the tested v0.60 contract in full; it adds no runtime
behavior requiring a new implementation test.

An initial invocation failed in setup because the basetemp parent directory did
not exist (23 setup errors). Creating the worktree-local directory corrected the
invocation; no source or test was changed to bypass a failure.

## Documentation and review scope

The five-file change consists of the normative freeze, this evidence record,
the P0 supersession notice, ROADMAP.md and docs/RELEASE_CHECKLIST.md. Check local
links, the incorporated contract, baseline ancestry, exact schema literals and
byte-exact preservation of historical P0 and shared planning tails. Production,
tests, dependencies, scanner allowances and runtime files remain unchanged.

Full Core, Mission Control and unrestricted worker release suites are not rerun
by this documentation Sync. The [v0.60 P5 evidence](v0.60-p5-validation.md)
and [release checklist](../RELEASE_CHECKLIST.md) retain all historical failures
and open acceptance gates. Passing focused tests cannot waive those obligations
or establish successor provenance, distinctness, feasibility or release authority.

## Results and hostile review

Focused tests passed: **23 passed, 327 warnings in 40.56s**, exit 0. The final
run is recorded locally in `.task-evidence/v061-sync/focused.log`; the selection
above is the portable reproduction record. Warnings are inherited.

Documentation checks passed: **142 local file links**, all new anchors, three
baseline ancestors, exact retained schema literals and full incorporation of the
unchanged v0.60 contract. Historical P0 and shared planning tails are byte-exact.
One pre-existing historical checklist anchor,
`DEPLOYMENT.md#atlas-v060-to-v07-upgrade-and-rollback`, does not resolve; its
file exists and the historical entry is preserved. No new broken link is added.
Local check script: `.task-evidence/v061-sync/check_docs.py`.

Hostile review passed: inspected the entire five-file change after the focused
run, including both new documents. Compared retention with the integrated
contract, reader/service semantics, SQLite metadata checks, guarded read errors,
UI deferral and P5 isolation evidence. Checked false success claims, schema/ID
invention, current GET status used as a stable pin, lineage/expiry changes,
widened bounds, replay, ownership escape, speculative phase authorization and
premature release closure. Seven blockers, 67 false fields and exact consumers
remain intact. No issue remains in this Sync scope; the historical anchor and
independent release obligations remain explicitly recorded.

`git diff --check` and explicit whitespace checks for new files passed. A
non-empty tracked diff plus the two new architecture documents establish the
repository implementation; this task does not require a release or commit.
