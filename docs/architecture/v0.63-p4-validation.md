# v0.63 P4 — retained Mission Control evidence integration

The [normative v0.63 boundary](v0.63-authority-boundary.md) retains the
Core-owned v0.57 interface prerequisite pair with the integrated v0.62 state.
Successor presentation remains deferred, so Mission Control continues to
expose only the existing exact-review nested, GET-only evidence projection.

The presentation now identifies the v0.63 disposition: runtime identity,
capability and lifecycle definition are not selected, and no new runtime
authority is established. Reader and presentation regressions reject invented
v0.63 version-label substitutions across every retained envelope. The
structural guard continues to forbid successor routes, write controls, polling
and operational consumers.

No schema, Core state, persistence, navigation, write control or production
consumer is added. Evidence remains nested beneath the exact review, with
collapsed details and blocked worker start and execution.

## Validation

Run from `services/mission-control` in the managed worktree after `npm ci`:

```sh
npx vitest run src/api/workerActivationRuntimeInterfacePrerequisite.test.ts src/features/installation/WorkerActivationRuntimeInterfacePrerequisite.test.tsx src/security/workerActivationRuntimeInterfacePrerequisiteStructural.test.ts --maxWorkers=2 --cache=false
npm run build
npm run lint
```

Focused tests passed: 3 files, 481 tests, exit 0. Build passed with the
existing bundle-size warning. Lint passed with zero errors and one existing
`WorkflowShellPage.tsx` hook-dependency warning. `git diff --check` passed.

Hostile review passed: the production diff only updates the retained boundary
explanation inside existing advanced evidence details. All state still comes
through the existing strict Core reader. Regression coverage includes v0.63
version-label substitution and the unchanged nested GET-only consumer
boundary. No dependency or lockfile changes are included.

This task does not reopen deferred successor implementation or claim release
closure, publication or deployment.
