# v0.61 P4 — retained Mission Control evidence integration

The [normative v0.61 boundary](v0.61-authority-boundary.md) retains the
Core-owned v0.57 interface prerequisite pair with integrated v0.60 hardening.
The [P3 API hardening](v0.61-p3-validation.md) adds no successor endpoint or
inventory/definition evidence. Mission Control's existing exact-review nested
reader already consumes that retained API. This change updates its advanced
explanation to the v0.61 disposition: successor runtime definition and
presentation remain deferred and no new runtime authority is established.

Focused regressions reject invented inventory-admission, abstract runtime
definition and definition-review markers (including false-valued markers) and
v0.61 version-label schema substitutions across collection, listed record, item
result, record, status and nested review envelopes. Presentation regressions
verify those responses reveal no evidence details. The structural guard also
forbids v0.61 successor routes and markers.

No schema, Core state, persistence, clock-derived eligibility, fingerprint
calculation, navigation, write control or production consumer is added. Evidence
remains a credentialed collection/item GET projection beneath the exact review,
with collapsed details and blocked worker start and execution. Browser-clock,
redacted item failure and equivalent-render regressions are retained unchanged.

## Validation

Run from `services/mission-control` in the managed worktree after `npm ci`:

```sh
npx vitest run src/api/workerActivationRuntimeInterfacePrerequisite.test.ts src/features/installation/WorkerActivationRuntimeInterfacePrerequisite.test.tsx src/security/workerActivationRuntimeInterfacePrerequisiteStructural.test.ts --maxWorkers=2 --cache=false
npm run build
npm run lint
npm test -- --maxWorkers=2 --cache=false
```

Focused tests passed: 3 files, 385 tests, exit 0. Build passed with an existing
bundle-size warning. Lint passed with zero errors and one existing
hook-dependency warning in `WorkflowShellPage.tsx`.

Full Mission Control tests passed: 174 files, 2,319 tests, exit 0. The local
full-suite log is `.task-evidence/v061-p4/full-test.log` at the repository root,
with the focused, build and lint logs in the same directory.

Hostile review passed: the production diff only updates the retained boundary
explanation inside existing advanced evidence details. All state still comes
through the existing strict Core reader. Regression coverage includes the v0.61
deferred markers at every retained envelope, version-label schema substitution,
browser clock independence, redacted item failure, exact-parent nesting,
scope-change races, fixed-false authority and the unchanged five-consumer UI
boundary. No dependency or lockfile changes are included. `git diff --check`
passed.

This task does not reopen deferred successor implementation or claim historical
release closure, publication or deployment.
