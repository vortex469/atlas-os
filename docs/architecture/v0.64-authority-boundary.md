# v0.64 Sync — retained authority boundary

Status: **Normative repository planning freeze. Retain the integrated v0.63
boundary and defer successor P1-P5. No successor implementation is
authorized.**

## Integrated baseline and decision

Inspected on 2026-09-20 at `43123ed58b4d96ed71fecb581b20593f2cdcdc2f`, the
repository-integrated v0.63 state and `atlas-v0.63.0` tag. The v0.64 P0
analysis is recorded at `72dadcab`; it is a provisional analysis, not a
runtime-authority change.

This Sync supersedes the v0.64 provisional selection with documentation-only
retention. The [v0.63 normative boundary](v0.63-authority-boundary.md)
remains binding in full: the Core-owned v0.57 prerequisite pair, complete
lineage, exact bounds, authority ceiling, consumer ownership and GET-only
Mission Control projection are unchanged. The v0.64 label creates no
evidence stage, schema, migration, marker, reader, route, permission,
credential, runtime client, process, container or deployment.

| v0.64 candidate | Frozen disposition |
| --- | --- |
| Another inventory/admission or consistency receipt | Deferred: the retained evidence does not become a distinct trusted decision by being wrapped again. |
| Runtime identity, capability and lifecycle definition | Deferred: trusted provenance, a distinct consumer decision and bounded feasibility remain unproved. |
| Runtime definition review or activation-plan review | Deferred: no accepted successor declaration exists to review. |
| Control-plane activation authorization envelope | Conditional research only: authenticated contact, exact approval, one-shot semantics and uncertainty recovery require a separate normative contract. |
| Runtime contact, worker start, Agent invocation or execution integration | Not selected: these are effect-bearing boundaries with independent authentication, permission and durable-effect obligations. |

## Retained output and authority ceiling

The accepted output remains `WorkerActivationRuntimeInterfacePrerequisiteV1` /
`WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, with the exact v0.63
schemas, inherited lineage, fingerprints, ownership, time and durability
bounds. The integrated v0.63 changes are retention and release-isolation
hardening only; they do not establish runtime activation feasibility or release
acceptance.

All seven blockers remain unresolved. Preserve `evidence_only=true`,
`reference_only=true`, `payload_bytes=0`, all 67 fixed-false authority/material
fields and the meanings of historical true evidence markers. No queue
claim/lease/ack/consume/requeue, store or runtime contact, activation, start,
Agent invocation, execution, installation, deployment, rollback, publication,
retry/resend or repair/replay authority follows. Mission Control remains a
nested, non-authoritative GET-only projection; Agent, worker and operational
consumers remain zero.

## Phase disposition and reopening

| Phase | Frozen v0.64 disposition |
| --- | --- |
| P1 | Successor contract/evaluator/vectors deferred; retain the integrated v0.63 contract. |
| P2 | Successor reader/service/journal deferred; retain the existing owned reader and bounded journal. |
| P3 | Successor API deferred; retain the existing guarded prerequisite API. |
| P4 | Successor presentation deferred; retain nested GET-only evidence. |
| P5 | Successor release closure deferred; predecessor acceptance remains independent and open. |

Reopening requires a separate normative decision proving an accepted baseline,
trusted and distinct decision provenance, exact owned pins, recursive
integrity, measured immutable limits, authenticated runtime contact, durable
one-shot/no-replay behavior, explicit uncertainty recovery and hostile
no-authority-promotion evidence. Release acceptance alone cannot select a
successor.

## Release and synchronization scope

Repository-integrated v0.63 and inherited release acceptance remain open where
recorded in the [release checklist](../RELEASE_CHECKLIST.md). Sync owns this
document, its [validation record](v0.64-sync-validation.md), the P0
supersession notice, `ROADMAP.md` and the release checklist. Production code,
tests, dependencies, deployment and operational consumers remain outside this
documentation freeze. No release, tag, publication or runtime enablement
follows.
