# v0.59 P5 retained authority isolation — release closure open

Baseline: `81913c6dbf1d89c3ff1a65e595dbe8507d75d476` (integrated P4).
The [normative contract](v0.59-runtime-interface-boundary.md) retains Core-owned
v0.57 prerequisite inventory hardened through v0.58 and v0.59. Definition-review
remains deferred. This task adds closure regressions and synchronized release
documentation; it adds no production authority or historical isolation exception.

## Exact integrated lineage

`git merge-base --is-ancestor <commit> HEAD` passed for every identity below.
Source integration is necessary but does not prove predecessor release acceptance.

| Source | Exact integrated commit |
| --- | --- |
| v0.58 P4 implementation | `f3609db51938b8df7343bcb0ca38d97f2d17f06e` |
| v0.58 P5 | `913bbc86cdcb59667c435d404537ea200df1b634` |
| v0.59 original Sync | `32dbe44360ba42539494408b68a9546f95ba37c3` |
| v0.59 retained P1 | `5b872aee450487625756d2e5b66977d5971342de` |
| v0.59 retained P2 | `06add49dd68cd0b9430aff5cc9ef19d00fecb22a` |
| v0.59 retained P3 | `7bb94c2e48333e6d59af4c0e720011a3df8f94ca` |
| v0.59 refreshed Sync | `72ab2abb70153cca1b88cb510920af1a740dd377` |
| v0.59 expanded-string bound | `67077c39fc395539a2f86886437d40e174b9ac52` |
| v0.59 P2 readback binding | `b7e8f8478f4c8ff36bf011ca75d7013c8e65f792` |
| v0.59 P3 no-store | `970ba1bb1f36735c10332e04c554d8b5922951f4` |
| v0.59 P4 | `81913c6dbf1d89c3ff1a65e595dbe8507d75d476` |

The actual evidence pair remains the exact v0.57 prerequisite record/status over
complete immutable v0.56 review/status. There is no synthetic v0.58 definition,
ID, wrapper, fingerprint domain or successor consumer. All six IDs, inherited
expiry, seven unresolved proofs and 67 fixed-false authority fields are retained.

## Regression coverage

The new `test_v059_release_closure.py` compares the seven ordered normative
blocker/owner/proof triples with Core and rejects both unselected admission and
definition-review markers, true and false, across seven envelope locations.
Positive controls first validate the complete Core/shared-UI golden result and
collection. Mutations must fail specifically with `extra_forbidden`, including
nested predecessor record/status. Core rejection does not substitute for UI test
execution. Production scanning rejects both successor families without reserved
paths, and also forbids retained prerequisite consumers in repository workflows.

The unchanged retained closure proves exact twelve-file Core/API/UI consumers,
zero Agent/worker consumers, byte-exact durable predecessor lineage, original
expiry, default-off behavior and permanent no-replay after restart. Selected
service/store regressions exercise corrupt indexes, missing/rebound journals,
independent owner/item/create readback binding and restart ownership. API checks
cover exact permissions/routes, no production construction and owner-scoped
no-store failures. Historical closure/isolation tests preserve blocked Home
Assistant evidence. Mission Control remains a GET-only nested non-authoritative
projection; no proof or downstream runtime/contact/start/execution authority is
created. No historical scanner, allowance or exclusion is changed.

## Current validation commands

Commands run from the managed worktree with the selected interpreter; temporary
files and logs are under `.task-evidence/v059-p5/`. That ignored directory is
local execution evidence; this document is the portable results record.

```bash
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v059_release_closure.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v058_release_closure.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py \
  --basetemp=.task-evidence/v059-p5/closure-tmp --tb=short --disable-warnings

PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_service_store.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py \
  services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py \
  -k 'v059 or restart_duplicate or corruption_closes or missing_live_journal or journal_path_binding or exact_registered_surface or production_startup or permissions_openapi or owner_scoped_failures or frozen_versioned_hash or normative_false_inventory' \
  --basetemp=.task-evidence/v059-p5/hardening-tmp --tb=short --disable-warnings

PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app \
  -k 'release_closure or mission_control_isolation or test_installation_release_isolation' \
  --ignore=services/atlas-core/app/worker_activation_runtime_interface_prerequisite \
  --basetemp=.task-evidence/v059-p5/history-tmp --tb=short --disable-warnings

PYTHONPATH=services/atlas-agent /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-agent/tests/test_delivery_activation_preflight_isolation.py \
  services/atlas-agent/tests/test_v036_installation_execution_admission_isolation.py \
  services/atlas-agent/tests/test_v037_runner_binding_plan_isolation.py \
  --basetemp=.task-evidence/v059-p5/agent-tmp

PYTHONPATH=services/atlas-agent:services/atlas-execution-worker \
  /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-execution-worker/tests/test_config.py \
  services/atlas-execution-worker/tests/test_healthcheck.py \
  services/atlas-execution-worker/tests/test_durable_ledger.py -k 'not api' \
  --basetemp=.task-evidence/v059-p5/worker-tmp
```

Focused tests passed: **45 passed, 350 warnings in 165.00s** for retained
v0.57/v0.58 and new v0.59 closure (`closure.log`). Final normative/consumer
checks after tightening the scanner: **2 passed, 28 deselected, 326 warnings
in 1.40s** (`final-scan.log`). Historical release/Mission Control/installation
isolation: **156 passed, 4278 deselected, 407 warnings in 158.42s**
(`history.log`). All three invocations exited 0.

Focused durable/contract/API hardening: **85 passed, 202 deselected, 336
warnings in 757.20s (12:37)**, exit 0 (`hardening.log`). This includes all
selected v0.59-specific cases plus restart, corruption, journal path, normative
authority/vector and exact API isolation checks.

Agent isolation: **6 passed in 0.06s**. Worker config/health/ledger:
**24 passed, 1 deselected, 1 warning in 0.47s**. The historical threaded worker
API exclusion is retained only in this invocation. This passing run does not
establish that the historical concurrent WAL initialization race is fixed.

The full retained package/API run historically took 43:49; this test/documentation
change reruns focused durable/API hardening and historical isolation instead.
No current full retained package, full repository or browser E2E pass is claimed.
Historical full results in [v0.58 P5](v0.58-p5-validation.md) remain historical.

## Required Mission Control gates — environment blocked

```bash
npm ci --prefix services/mission-control \
  --cache "$PWD/.task-evidence/npm-cache" --no-audit --no-fund \
  --fetch-retries=0 --fetch-timeout=15000
npm ci --offline --prefix services/mission-control \
  --cache "$PWD/.task-evidence/npm-cache" --no-audit --no-fund
npm test --prefix services/mission-control -- --maxWorkers=2 --cache=false
npm run build --prefix services/mission-control
npm run lint --prefix services/mission-control
```

Environment error: Node 22.23.1/npm 10.9.8 are present, but online `npm ci`
exits 1 with `Exit handler never called!`; offline installation exits 1 with
`ENOTCACHED` for `zod-validation-error`. Vitest and ESLint are unavailable (test
and lint exit 127). Build exits 1 because Vite/Node types are missing and fallback
TypeScript does not support `erasableSyntaxOnly`. No dependency manifest or
lockfile is modified. These failures do not weaken or discharge the release gates.

The repository-integrated v0.58 P5 record explicitly leaves release closure open.
Current P5 cannot close v0.59 until predecessor closure and all required current
validation exist. Historical P4 focused UI success is insufficient. Restore locked
dependencies and rerun full UI tests/build/lint, then reassess release closure.
No tag, publication, deployment or runtime enablement is claimed.

## Hostile review and commit evidence

Hostile review passed: reviewed the full six-file diff after all focused tests
completed. Checked positive golden controls, reason-specific nested rejection,
true/false successor markers, all marker naming forms, exact consumer ownership,
unchanged expiry/replay semantics and release prerequisites. The production scan
also explicitly rejects the snake-case admitted marker; its final two-test scan
was rerun after that tightening. No production, historical isolation, dependency,
deployment or runtime file changed. No unresolved defect was found in this diff.
Required UI release validation remains blocked independently.

Ruff check and formatting passed for the new Python test. Documentation checks
validated 159 local link targets and byte-exact historical document tails; all
11 selected ancestors passed. Staged and unstaged whitespace checks passed.

Normal Git staging initially succeeded, but final staging and commit failed with
exit 128: the managed worktree index lock is on a read-only filesystem. Permission
escalation is unavailable. The managed branch was not advanced.

The task-owned real commit is instead stored in the writable worktree at
`.task-evidence/v059-p5/commit.git`, with the exact checkout baseline as parent,
and exported as `.task-evidence/v059-p5/implementation.bundle`. Its title is
`test: record v0.59 retained authority isolation and release gates`. This follows
the integrated P4 artifact approach; no original Git metadata is relocated or
modified by the artifact creation. The repository diff remains available for
integration. The commit records open release gates, not release acceptance.
