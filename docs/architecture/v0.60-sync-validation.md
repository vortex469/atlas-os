# v0.60 Sync validation — retained repository boundary

Baseline: `5ece1b5226a588d6d99c3469d2d864022e67d1ef` (2026-09-14).
The [normative freeze](v0.60-runtime-definition-boundary.md) reconciles integrated
P0 `00d586ac`, its baseline refinement and v0.59 P5 `d66bb00a`. The sole
selected boundary retains Core-owned v0.57 inventory with integrated v0.59
hardening. Runtime definition P1-P5 are deferred; no production work is selected.

## Focused validation

Run from the managed task worktree using the selected Python interpreter's
`-m pytest`, with `PYTHONPATH=services/atlas-core`:

```bash
mkdir -p .task-evidence/v060-sync
# Use the selected Python interpreter with -m pytest -q and these arguments:
# services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v059_release_closure.py
# services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py
# services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py
# services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py
# -k 'v059_normative or v059_no_successor or exact_v057_production_consumers or normative_inventory_authority or frozen_versioned_hash or normative_false_inventory or permissions_openapi_method or exact_registered_surface or production_startup'
# --basetemp=.task-evidence/v060-sync/pytest-tmp --tb=short --disable-warnings
```

Focused tests passed: **9 passed, 181 deselected, 327 warnings in 11.30s**.
These check retained normative inventory, independent fingerprint/UUID vectors,
all 67 false fields, exact twelve production consumers, no successor/workflow
consumer, exact API methods/permissions and no production service construction.
Warnings are inherited, including Pydantic schema-shadowing warnings.

A worktree-local documentation check compares the new v0.60 inventory directly
with Core's seven ordered blocker/owner/proof triples, both actual schema
literals and all eight owner/candidate/UUID identity fields on each model.
It verifies the three dependency/baseline ancestors, resolves local links within
the managed worktree, and checks byte-exact preservation of P0 and shared
historical planning tails. The script is local execution evidence at
`.task-evidence/v060-sync/check_docs.py`; this document is the portable record.

This documentation Sync does not rerun full Core/API durability, full Mission
Control tests/build/lint, browser or repository suites. The
[v0.59 P5 evidence](v0.59-p5-validation.md) records historical dependency failures
and open v0.58/v0.59 release closure. No fresh environment failure or release
acceptance is inferred here; those gates remain independent and unwaived.

## Review and scope

The five-file scope comprises normative v0.60 architecture, its validation
record, a P0 supersession notice, ROADMAP.md and docs/RELEASE_CHECKLIST.md.
No production, test, scanner allowance, dependency, runtime or deployment changes
belong to this freeze. The conceptual definition lacks trusted provenance,
distinct predicate/consumer evidence and measured successor bounds; retention
is the supported P0 fallback, not an invented implementation contract.

Documentation checks passed: seven ordered triples, two schemas, eight
scope/identity fields per model, three ancestors, 137 local links and byte-exact
historical tails. `git diff --check` passed.

Hostile review passed: reviewed all five files after focused tests, comparing
retained schema, identity, hash, time, service/readback and API statements with
integrated code and predecessor evidence. Checked for invented definition pairs,
unstable GET fingerprints used as stable pins, receipt-ID invention, lineage
truncation, increased limits, expiry renewal, replay, ownership escape, discharged
proofs and premature release/phase authorization. No defect remains in this
scope. The documentation check covers the new normative table directly; existing
tests cover the retained code, so no implementation-mirroring test was added.

The required task commit uses title
`docs: freeze v0.60 against integrated v0.59 Core/API boundary`.
It records this five-file documentation decision, not release acceptance.

Commit blocker: `git add` exited 128 because the managed worktree Git index
lock is on a read-only filesystem. This session cannot escalate permissions.
No commit was created and no alternate metadata was substituted. The five-file
repository change remains reviewable in the managed worktree; the required real
commit remains outstanding, so the task is not fully complete.
