# v0.61 P0 — Provisional next authority boundary analysis

Status: **Provisional planning only; retain the existing boundary. No successor
is selected and successor P1-P5 remain deferred.** Inspected on 2026-09-14 at
`4cebd6ab681937be913a9f2900644315a4e7430d`, with integrated v0.60 P5
`819e17705e11721ff8c0e13e973b31f9b67c79f7` verified as an ancestor.
Source integration does not establish predecessor release acceptance.

This card owns only this analysis document. The
[v0.60 normative freeze](v0.60-runtime-definition-boundary.md) continues to
govern production. This P0 neither supersedes it nor revives deferred runtime
definition, definition-review or inventory-admission implementation. Shared
roadmap/release synchronization belongs to a separately scoped decision.

## Evidence at the integrated baseline

| Source | Observed consequence for v0.61 |
| --- | --- |
| [v0.60 P0](v0.60-provisional-runtime-definition-boundary.md) and normative freeze | Abstract runtime definition was a hypothesis. Trusted declaration provenance, a distinct predicate/consumer and bounded successor feasibility remain unproved. Renumbering that hypothesis supplies no new evidence. |
| [v0.60 P5 evidence](v0.60-p5-validation.md), [release checklist](../RELEASE_CHECKLIST.md) | Retained contract, SQLite metadata, guarded API and nested UI hardening are integrated. Predecessor acceptance, current full UI gates, full Core lint/validation and unrestricted worker API validation remain recorded as open. Historical failures are not fresh environment diagnoses by this P0. |
| [Core contract](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/contract.py) | The accepted pair remains `WorkerActivationRuntimeInterfacePrerequisiteV1` / `WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, with v0.57 schemas and the fixed seven-entry inventory over v0.56 review. No v0.58, v0.59 or v0.60 evidence pair exists. |
| [Owned reader](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/readers.py) and [service](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/service.py) | The reader consumes exact owned v0.56 evidence and derives stable status at its recorded time. Item GET derives current status; list GET supplies records without paired statuses. Neither supplies a successor reader of v0.57. |
| [Retained closure](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py), [v0.60 closure](../../services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v060_release_closure.py) | Exact twelve production consumers and zero operational consumers remain enforced. Definition claims, including false-valued claims, are rejected in shared envelopes. This P0 grants no scanner exception. |
| [Integrated Worker / Execution overview](../../services/mission-control/src/features/mission-control/WorkerExecutionSection.tsx) | Persisted Agent workflow observations explicitly grant no execution authority. Worker heartbeat and aggregate queue/admission evidence are unavailable there; empty activity cannot establish idleness or readiness. Recent dashboard integration supplies no installation runtime proof. |
| [Execution worker contract](../../services/atlas-execution-worker/README.md) | The optional repository execution backend has its own authentication and no-replay ledger. Packaging, health and backend activation do not expand installation authority or supply the missing installation interfaces. |

The exact immutable lineage remains v0.57 inventory -> v0.56 review -> v0.55
plan -> v0.54 runtime admission -> v0.53 runtime prerequisite -> v0.52 queue
receipt -> v0.51 acknowledgement admission -> v0.50 prerequisite -> v0.49
claim admission -> all inherited v0.48-v0.20 pairs in the
[v0.55 contract](worker-activation-runtime-plan-v1.md). The v0.52 receipt
records injected already-observed assertions, not independently verified Core
queue effects. Repository versions and Git commit IDs are not evidence IDs.

## Candidate comparison and provisional disposition

| Candidate | Decision and missing evidence |
| --- | --- |
| Another inventory admission or consistency review | Defer: fixed inventory correspondence is already validated. No additional consumer decision is demonstrated. |
| Abstract installation runtime identity, capability composition and lifecycle definition | Keep as the conditional research candidate: potentially supplies relationships absent from the inventory. It must first prove trusted provenance, distinctness and bounded usefulness. No persisted stage is selected. |
| Review of a runtime definition | Defer: there is no accepted definition pair to review. Do not synthesize a v0.60 predecessor. |
| Store/runtime contact contract or adapter | Defer: separate storage/runtime owners must establish authentication, exact subjects, permitted operations, effect limits and recovery. A Core evidence journal cannot supply these proofs. |
| Worker-start admission/start or Agent/execution integration | Blocked by unresolved runtime/contact proofs and missing exact downstream approval and durable effect boundaries. Workflow observations cannot bridge them. |

The first unresolved proof, owned by `core_installation_authority`, is
`installation_runtime_identity_capability_composition_lifecycle`. Its declarative
scope makes it a plausible next investigation without contacting a worker. The
inventory order does not prove dependency order or justify another evidence
wrapper. The immediate disposition is documentation-only retention until a
separate normative decision establishes a useful advance.

The prospective trust crossing is from authenticated caller references and a
trusted closed declaration source into Core installation evidence evaluation.
Core would own canonical identity, validation, trusted time and persistence.
Caller prose, AI suggestions, discovered endpoints, credentials, command bytes
and executable configuration cannot serve as trusted declarations. Abstract
identity does not authenticate a live worker. Mission Control remains a
non-authoritative GET-only projection; browser time and cached success confer no
eligibility. Agent, worker, relay and operational workflows gain no consumer.

## Required evidence before normative selection

These are open handoff gates, not results of this analysis:

1. **Accepted predecessor:** pin the final integration commit, governing contract
   and acceptance evidence; explicitly resolve the recorded v0.60 and inherited
   release obligations. A passing P0 test or later dashboard merge cannot close
   them. If the predecessor changes, re-analyze its actual pair rather than using
   an older-stage fallback. Closing release gates alone does not select a successor.
2. **Distinct decision and provenance:** provide two declarations for the same
   exact inventory input, one internally consistent and one whose proposed
   capability conflicts with a lifecycle constraint. Identify the trusted author
   and deterministic source of that constraint and the concrete consumer decision
   changed by the distinction. These are conceptual cases, not approved protocol
   literals. Specify refusal versus a closed negative finding; no silent repair.
   If v0.55 design, v0.56 review or v0.57 validation already distinguishes them,
   retain documentation only.
3. **Owned stable predecessor:** under the retained outcome, a separately approved
   reader must bind authenticated `operator_id`, `candidate_record_id`, exact
   `runtime_interface_prerequisite_id`, record fingerprint, stable status
   fingerprint and inherited expiry. Derive stable status at inventory
   `recorded_at`, independently checking current eligibility at trusted whole-second
   UTC Core time. Arbitrary item-GET status is not a stable pin; a list item is
   not a record/status pair. Refuse foreign ownership, substituted fingerprints,
   expired lineage, caller-supplied nested evidence and latest/worker lookup.
4. **Recursive integrity and feasibility:** retain complete immutable lineage,
   all six predecessor IDs and version-specific hashes, subjects and historical
   times. V0.52 shares v0.51 `admission_id`; v0.54 has `runtime_admission_id`.
   Reparse copy/construct bypasses; reject extra/duplicate fields and coercion.
   Measure expanded full record, status, result and collection bytes and evaluation
   duration for the exact proposed embedding, including over-bound refusal.
   Existing limits remain 192 KiB per full model/response, 30-second freshness,
   16 reservations per owner, 256 globally and 256 MiB main SQLite pages, not a
   filesystem quota. Do not truncate lineage, enlarge limits or renew expiry to
   fit a successor. No successor size/time measurement is claimed here.
5. **Durability and bounded uncertainty:** freeze successor domains, vectors and
   permanent owner/candidate/exact-predecessor subject only after selection.
   Subject identity must exclude key, content and expiry. Require default-off
   explicit construction, locked revalidation, reservation before append,
   terminal uncertainty and restart no-replay without predecessor journal mutation.
6. **No authority promotion:** both declaration examples must leave operational
   proof unresolved. Test consistency success with missing runtime/contact proof,
   forged lineage, stale/foreign status, true and false invented successor fields,
   and forbidden payload material. A recorded declaration cannot discharge any
   blocker or create runtime readiness.

If any gate is unproved, keep P1-P5 deferred. If a proposal needs contact or
effects, it requires a separate authority design rather than expanding this
reference-only candidate.

## Preserved authority ceiling

The exact blocker/owner/proof triples remain those in the normative v0.60 freeze
and Core inventory. All seven blockers remain unresolved:

- `worker_activation_runtime_not_defined`
- `store_contact_not_defined`
- `runtime_contact_not_defined`
- `worker_start_admission_not_defined`
- `worker_start_not_defined`
- `agent_invocation_not_defined`
- `execution_start_boundary_not_defined`

Preserve `evidence_only=true`, `reference_only=true`, `payload_bytes=0` and all
67 fixed-false authority/material fields from the v0.55 authority ceiling;
historical true markers retain only their original evidence meanings.
No queue claim/lease/ack/consume/requeue, store/runtime contact, activation,
start admission/build/start, Agent invocation, execution authorization/start,
shell/process/container, scheduling, provider/repository/guest mutation,
installation, deployment, rollback, publication, retry/resend or repair/replay
bypass follows. Home Assistant remains blocked without installation artifacts.

## Conditional phase ownership

No schema, marker, UUID/hash domain, package, API, permission or consumer path is
reserved. Exact paths and shared-file ownership require normative reconciliation
before any implementation; historical scanner allowances must stay exact.

| Phase | Conditional responsibility and exit evidence |
| --- | --- |
| P1 | Core pure closed declaration models/evaluator and independent vectors, only after a distinct predicate is selected; hostile lineage, authority, inconsistency and bound checks. |
| P2 | Core exact owned predecessor reader and separate default-off service/journal; byte-exact lineage, owner-bound readback, corruption closure, unchanged predecessor storage and restart no-replay. |
| P3 | Guarded Core evidence API only if justified; separately coordinate router and permissions, strict framing, Origin/CSRF, idempotency, owner-bound no-store responses and redacted errors. No runtime construction. |
| P4 | Nested GET-only Mission Control evidence beneath the exact prerequisite; separately coordinate its existing installation parent, strict envelopes, stale-request cancellation and visible unresolved blockers. No action controls, polling, persistence or standalone navigation. |
| P5 | Exact approved consumer and durable isolation regressions, zero Agent/worker consumers, required Core/API/UI validation and explicit predecessor/release acceptance. No release publication is implicit. |

No conditional phase is completed here. Production, dependencies, deployment,
shared release records and historical validation remain unchanged.

## P0 validation

Run retained v0.60 envelope/consumer regressions and the v0.57 exact-consumer,
normative inventory and fixed-false authority checks. Verify this document's
local links and whitespace, then review the diff for sole-document ownership,
unsupported release claims, duplicate stages and accidental authority promotion.
These checks establish planning consistency, not successor feasibility or full
Core/UI/worker release acceptance.

Validation performed on 2026-09-14 from the managed worktree using the selected
interpreter's `-m pytest -q`, `PYTHONPATH=services/atlas-core`,
`--basetemp=.task-evidence/v061-p0/pytest --tb=short --disable-warnings`.
The test selection, relative to
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`, was:

- `test_v060_release_closure.py` (all 15 cases)
- `test_v060_retained_boundary.py::test_v060_freeze_retains_exact_inventory_and_schemas`
- `test_release_closure.py::test_exact_v057_production_consumers`
- `test_release_closure.py::test_normative_inventory_authority_and_downstream_ceiling`
- `test_contract.py::test_normative_false_inventory_is_exact`

Focused tests passed: **19 passed, 326 warnings in 31.25s**, exit 0. All 12
local document links resolve to files inside this worktree; whitespace checks
passed. A non-empty Git comparison against an empty worktree-local file confirms
the added document; no production change is needed for this provisional P0.

Hostile review passed: inspected the complete addition after tests, checked
baseline ancestry, retained pair/reader semantics, dashboard versus installation
authority, duplicate-stage risk, unproved selection gates and conditional phase
ownership against the cited source. Seven blockers, 67 false fields and exact
consumer restrictions remain intact. No predecessor release gate is waived and
no speculative declaration is presented as implemented or trusted. Existing
regressions cover the retained claims; a new runtime test or consumer allowance
would be outside this documentation-only change. Full release gates were not
rerun by this card and remain separate obligations.
