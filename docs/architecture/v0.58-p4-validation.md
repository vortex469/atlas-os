# v0.58 P4 retained Mission Control evidence

The [frozen v0.58 contract](v0.58-runtime-interface-admission-provisional.md)
retains the v0.57 Core interface-prerequisite inventory and defers a separate
admission stage. This change improves the existing reader nested beneath the
exact runtime plan review; it does not introduce a successor evidence consumer.

The operator view leads with blocked worker start and execution. Recorded
inventory explicitly does not satisfy prerequisites, define an interface or
authorize contact/start. Active/expired evidence remains Core's last evaluated
state. IDs, fingerprints, exact lineage, inventory, reason codes and fixed-false
authority stay in collapsed advanced details, alongside the v0.58 deferral context.
No reservation state is invented from evidence markers or duplicate metadata.

Focused regressions reject an invented successor admission field, whether true
or false, in collection, listed record, result, item record and status envelopes.
The component regression confirms such a response displays only unavailable
evidence and blocked state. Presentation checks keep technical evidence hidden
initially. Existing tests cover exact parent nesting, owner/candidate scope changes,
late responses, redacted failures, expiry, strict authority and Core golden evidence.
Structural checks retain exactly the five existing production UI consumers and
credentialed GET-only access, without polling, persistence or standalone navigation.

## Validation

From `services/mission-control` in the managed task worktree:

```bash
npm test -- src/api/workerActivationRuntimeInterfacePrerequisite.test.ts src/features/installation/WorkerActivationRuntimeInterfacePrerequisite.test.tsx src/security/workerActivationRuntimeInterfacePrerequisiteStructural.test.ts src/features/installation/WorkerActivationRuntimePlanReview.test.tsx
npm run build
npm run lint
```

Focused tests passed: **276 tests across 4 files**, exit 0 (17.19 seconds).
Build passed, exit 0, with a bundle-size warning. Lint passed, exit 0, with one
existing `react-hooks/exhaustive-deps` warning in `WorkflowShellPage.tsx:87`.
Dependencies installed using the existing lockfile; no dependency files changed.

Hostile review passed: inspected the complete implementation/test diff after the
checks, including parent nesting and hook cleanup. Checked that the UI neither
promotes historical evidence to readiness nor computes current eligibility;
unknown authority fails closed, errors remain redacted, exact scope changes clear
old evidence, and technical details remain collapsed. No additional production
consumer, route, authority or action was introduced. `git diff --check` passed.

These are focused local UI gates, not full-suite, browser E2E, deployment or
release acceptance. Admission P1-P5 remain deferred under the normative contract.
