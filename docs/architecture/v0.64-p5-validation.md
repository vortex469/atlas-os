# v0.64 P5 — authority isolation and release evidence

P5 closes the integrated v0.64 P1-P4 reference-only runtime-definition
boundary. The contract and journal record bounded evidence only; the guarded
API is default-off and Mission Control is a nested GET-only projection.

## Focused validation

`test_v064_release_closure.py` rejects unselected inventory, review, activation
and worker-start markers in the shared evidence envelopes, checks the exact
reference-only authority ceiling, and scans Agent, execution-worker,
configuration, deployment, scripts and workflows for effect-plane consumers.

The existing v0.64 contract and service/store tests continue to cover closed
schemas, fingerprints, owner binding, permanent subject reservation,
restart-safe idempotency and default-off behavior.

Focused tests passed: **13 passed**, exit 0. `git diff --check` passed.

## Release disposition and hostile review

Release acceptance remains open pending the inherited full Core/worker gates.
This evidence is not a release, tag, publication, deployment, runtime start or
Agent invocation.

Hostile review passed: the closure checks reject both true and false forged
authority markers, preserve the selected reference-only fields, and inspect
all effect-plane production areas without treating test fixtures as consumers.
No credential, runtime contact, process, container or operational consumer was
introduced.
