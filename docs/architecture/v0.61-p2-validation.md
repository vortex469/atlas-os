# v0.61 P2 request — retained bounded durable evidence

The [normative v0.61 freeze](v0.61-authority-boundary.md) retains the Core-owned
v0.56 reader and v0.57 prerequisite service/journal, including the integrated
[v0.60 P2 bounds hardening](v0.60-p2-validation.md). The requested bounded,
durable append-only implementation already exists. No production correction
was needed. Successor P2 remains deferred; this change adds regression evidence
for the retained implementation and does not select a successor journal.

`test_v061_durable_evidence.py` adds eight cases:

- Successful evidence, audited append failures and interrupted bare reservations
  each survive two restarts under both owner and global capacity limits.
- Successful duplicates return the exact original record. Failed/interrupted
  duplicates remain indeterminate, and different keys cannot replay the subject.
- All three states consume capacity permanently. New candidates are refused at
  capacity before predecessor reads, including a different owner at global
  capacity. Restart and refusals leave the SQLite file byte-identical.
- Empty and populated journals retain application ID 57. Relabeling them as 61
  fails closed on live list, idempotency lookup and restart, without migration
  or changes to the damaged file.

The tests build complete predecessor lineage through the existing contract
fixtures and invoke the real service/store. Exceptions are injected only at the
terminal append boundary to distinguish durable failure audits from interruption
before audit. The metadata suite also checks bounded schema readback and exact
schema acceptance on restart.

## Validation

Run from the managed worktree with the selected interpreter and
`PYTHONPATH=services/atlas-core`, using `-m pytest -xq --tb=short
--disable-warnings`, against these files in
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`:

- `test_v061_durable_evidence.py`
- `test_v060_store_bounds.py`

Use `--basetemp=.task-evidence/v061-p2/pytest-tmp`. Local output is retained in
`.task-evidence/v061-p2/pytest.log`. Focused Ruff lint and formatting checks cover
the new Python file.

Focused tests passed: **13 passed, 326 warnings in 266.04s**, exit 0.
Ruff lint and formatting checks passed for the new test file.

Hostile review passed: reviewed the complete diff after test success for replay,
capacity loss, false-positive rejection, fixture contamination, accidental
migration and authority expansion. Positive successful appends precede duplicate
and populated-corruption checks. Owner/global limits are exercised separately;
restart uses fresh, unpatched store instances, so injected append failures cannot
mask replay attempts. Exact error codes, row counts, forbidden predecessor reads
and file-byte comparisons independently constrain outcomes. All journal mutations
are confined to temporary test files. The change adds no production consumer,
schema, migration or runtime effect. `git diff --cached --check` passed.

Full API, UI, consumer isolation and release acceptance gates are outside this
retained P2 regression change; the freeze's deferred successor phases and
historical release obligations remain open.
