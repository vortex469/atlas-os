# v0.61 Sync — retained authority boundary

Status: **Normative repository planning freeze. Retain the Core-owned v0.57
prerequisite record/status pair with repository-integrated v0.60 hardening.
Successor P1-P5 remain deferred. No successor implementation is authorized.**

## Integrated baseline and decision

Inspected on 2026-09-14 at `827c751d403a7537e7d076f327f50b89e8336ba7`,
which integrates [v0.61 P0](v0.61-provisional-authority-boundary.md).
Its inspected baseline `4cebd6ab681937be913a9f2900644315a4e7430d` and
v0.60 P5 `819e17705e11721ff8c0e13e973b31f9b67c79f7` are verified ancestors.
The [v0.60 P5 record](v0.60-p5-validation.md) pins the integrated P0/Sync and
retained P1-P4 commits. Source integration does not establish release acceptance.

This Sync supersedes P0's provisional selection and conditional phase proposals
with documentation-only retention. The [v0.60 normative boundary](v0.60-runtime-definition-boundary.md),
including its P1 and P2-P5 retention reconciliation, is incorporated in full as
the technical contract for v0.61. Its exact schemas, identities, fingerprint/UUID
domains and vectors, recursive lineage, time, durability, API, ordered inventory,
authority ceiling and consumer ownership remain binding. The version label
creates no evidence stage, migration, schema, marker, domain, reader or route.

| Candidate from P0 | Frozen disposition |
| --- | --- |
| Inventory admission or consistency review | Deferred: existing validation already checks correspondence; no distinct consumer decision is demonstrated. |
| Abstract runtime identity/capability composition/lifecycle definition | Deferred: trusted declaration provenance, a distinct predicate/consumer and bounded embedding feasibility remain unproved. The first unresolved inventory proof is an investigation target, not an accepted declaration contract. |
| Runtime definition review | Deferred: no definition pair exists to review; v0.60 is hardening, not a predecessor evidence ID. |
| Store/runtime contact or adapter | Requires a separate authority design and authenticated subject, operations, effect and recovery proofs. No contact is selected. |
| Worker start, Agent or execution integration | Blocked by unresolved runtime/contact proofs and missing exact approval/durable effect boundaries. Dashboard observations confer no installation authority. |

## Actual retained output

The accepted pair remains `WorkerActivationRuntimeInterfacePrerequisiteV1` /
`WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, with schemas
`worker-activation-runtime-interface-prerequisite-v1` and
`worker-activation-runtime-interface-prerequisite-status-v1`.
It embeds the complete v0.56 review record/status and inherited v0.55-v0.20
lineage. All six predecessor/stage UUID5 fields, owner/candidate binding and
version-specific hashes remain exact. V0.52 shares v0.51 `admission_id`;
v0.54 uses `runtime_admission_id`. Git commits are not evidence identities.

| Integrated retention | Binding consequence |
| --- | --- |
| [P1 contract regressions](v0.60-p1-validation.md) | Existing pure closed models/evaluator reject invented definition authority, even false-valued claims and copy/construct bypasses. No definition contract was implemented. |
| [P2 store hardening](v0.60-p2-validation.md) | SQLite metadata row counts, storage classes and UTF-8 name/SQL lengths are checked before Python materialization; exact schema equality and terminal corruption remain. This does not bound SQLite internal parsing memory. |
| [P3 guarded reads](v0.60-p3-validation.md) | Authentication remains separate from unexpected dependency HTTP failures, which receive redacted, bounded, no-store, non-retryable errors. Exact owner-bound API methods/permissions remain unchanged. |
| [P4 nested evidence](v0.60-p4-validation.md) | The existing GET-only view explains definition deferral; Core owns lifecycle eligibility despite browser clock differences. No action control or consumer is added. |
| [P5 isolation](v0.60-p5-validation.md) | Shared envelopes reject true and false definition claims; exact twelve production consumers and zero operational consumers remain enforced. Historical release obligations stay open. |

The owned reader still consumes exact v0.56 evidence and derives stable status
at review `recorded_at`, independently checking current eligibility at trusted
whole-second UTC Core time. Item GET derives current v0.57 status; list GET
returns records without paired statuses. Neither is a stable successor reader.
No latest/worker lookup, foreign owner, caller-provided nested evidence, lineage
truncation or expiry renewal is permitted.

Default-off explicit service construction, locked revalidation, permanent
owner/key and subject reservations before append, terminal uncertainty and
restart no-replay remain. Preserve 192 KiB per full model/response, 30-second
freshness, 16 reservations per owner, 256 globally and 256 MiB main SQLite pages
(not a filesystem quota). No successor size/time measurement is claimed.

## Unchanged authority ceiling

The exact seven ordered blocker/owner/proof triples in the incorporated
[v0.60 inventory](v0.60-runtime-definition-boundary.md#fixed-inventory-and-authority-ceiling)
remain unresolved. No proof is discharged. Preserve `evidence_only=true`,
`reference_only=true`, `payload_bytes=0` and all 67 fixed-false authority/material
fields; historical true markers retain only their original evidence meanings.

No queue claim/lease/ack/consume/requeue, store/runtime contact, activation,
start admission/build/start, Agent invocation, execution authorization/start,
process/shell/container, scheduling, provider/repository/guest mutation,
installation, deployment, rollback, publication, retry/resend or repair/replay
bypass follows. Home Assistant remains blocked without installation artifacts.
Mission Control remains a non-authoritative nested GET-only projection with no
polling, persistence or standalone navigation. Integrated Worker / Execution
workflow observations and the optional execution backend's own ledger do not
supply installation runtime proofs or expand the exact consumer allowance.

## Phase disposition and reopening

| Phase | Frozen v0.61 ownership/disposition |
| --- | --- |
| P1 | Successor contract/evaluator/vectors deferred; retain existing Core contract. |
| P2 | Successor owned reader/service/journal deferred; retain v0.56 reader and v0.57 journal. |
| P3 | Successor API deferred; retain existing guarded prerequisite create/list/get. |
| P4 | Successor presentation deferred; retain the nested GET-only evidence view. |
| P5 | Successor release closure deferred; predecessor acceptance remains an independent obligation. |

Retained v0.60 P1-P5 work does not complete these successor phases. Reopening
requires a separate normative decision satisfying all six
[P0 evidence gates](v0.61-provisional-authority-boundary.md#required-evidence-before-normative-selection):
accepted predecessor; consistent/conflicting declarations with trusted constraints
and a distinct consumer decision; exact owned stable predecessor pins and current
eligibility; complete recursive integrity and measured full record/status/result/
collection byte and evaluation-time bounds; permanent subject/durability/no-replay;
and hostile no-authority-promotion evidence. Fix refusal versus closed negative
finding, exact phase paths and consumer ownership before implementation. Do not
widen limits, truncate lineage, reserve scanner exceptions or infer selection
from release closure alone. If any gate remains unproved, retention continues.

## Release and synchronization scope

V0.60 and inherited release acceptance remain open. The
[release checklist](../RELEASE_CHECKLIST.md) retains predecessor acceptance,
current full Mission Control tests/build/lint, full Core Ruff/validation and
unrestricted worker API validation. P5's dependency failures, ownership-fixture
limitation and stalled worker invocation are historical observations here, not
fresh environment diagnoses or waived gates. Focused Sync checks cannot close
them, and historical P4 UI success cannot replace current release validation.

Sync owns this document, its [validation record](v0.61-sync-validation.md), the
P0 supersession notice, `ROADMAP.md` and `docs/RELEASE_CHECKLIST.md`. Historical
planning/evidence is preserved. Production, tests, scanner allowances,
dependencies and deployment remain outside this documentation freeze. This
completes the v0.61 planning decision only; no release, tag or publication follows.
