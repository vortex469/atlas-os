# v0.63 Sync validation — integrated v0.62 retention

Inspected baseline: `426cac84c7d4a287684752f38e8284e01d65dea2` (2026-09-20).
The [normative freeze](v0.63-authority-boundary.md) reconciles v0.63 P0 at
`24dfdeeb` with the integrated v0.62 state. Successor P1-P5 remain deferred;
no release acceptance follows.

## Focused validation

Run from the managed worktree with the selected interpreter's `-m pytest -q`,
`PYTHONPATH=services/atlas-core`, and
`--basetemp=.task-evidence/v063-sync/pytest --tb=short --disable-warnings`.
The focused selection is the retained v0.62 boundary, closure, durable
evidence, base contract, exact-consumer, authority-ceiling and fixed-false
contract tests under
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`.
Documentation checks additionally verify local links, the baseline tag/commit
and whitespace.

The Sync changes are documentation-only. The v0.62 closure and execution-worker
durability fix are retained evidence; no source is changed to bypass a failure,
and full release gates are not rerun here.

## Results and hostile review

Focused tests passed: **237 passed, 326 warnings in 928.56s**, exit 0. The
parent of the documented `--basetemp` path was created before invocation.

Documentation checks passed: local links, baseline references and changed-file
whitespace passed.

Hostile review passed: yes. Inspected the complete Sync diff against the v0.62
freeze for invented provenance, duplicate stages, widened bounds,
execution-worker authority promotion, consumer exceptions and premature
release closure. No production behavior or successor authority is introduced.
