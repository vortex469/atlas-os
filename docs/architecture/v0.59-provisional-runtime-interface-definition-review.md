# v0.59 P0 — Provisional runtime interface definition review evidence

> Historical P0 evidence. The [normative v0.59 Sync contract](v0.59-runtime-interface-boundary.md)
> supersedes the provisional assumptions and conditional phase proposals below.
> The retained prerequisite boundary is frozen; definition-review P1-P5 are deferred.

Status: **Provisional architecture only. No normative selection, final v0.58
lineage, implementation authorization, or P1-P5 completion is claimed.**

## Inspected baseline and uncertainty

Inspection starts at commit `1371cd369c2dc416eae7b9d4bdac504700888935`.
The checkout contains v0.56 runtime plan review implementation and the
[provisional v0.57 prerequisite proposal](worker-activation-runtime-interface-prerequisite-v1.md).
No v0.58 planning contract or implementation was found in the tracked checkout.
Absence here does not establish the state of concurrent branches or external
release work. This document can coexist with that work; it neither assigns a
final v0.58 boundary nor treats the v0.57 proposal as implemented.

| Repository evidence inspected | Consequence for this proposal |
| --- | --- |
| [v0.55 plan](worker-activation-runtime-plan-v1.md) and [model](../../services/atlas-core/app/worker_activation_runtime_plan/contract.py) | A reference-only plan preserves unresolved runtime and contact interfaces. A plan is not a runnable protocol. |
| [v0.56 contract](worker-activation-runtime-plan-review-v1.md) and [model](../../services/atlas-core/app/worker_activation_runtime_plan_review/contract.py) | Review currently covers exact plan lineage, fixed design consistency and preserved unresolved interfaces. Repeating those findings adds no distinct boundary. |
| [v0.56 owned reader](../../services/atlas-core/app/worker_activation_runtime_plan_review/readers.py), [service](../../services/atlas-core/app/worker_activation_runtime_plan_review/service.py), [store](../../services/atlas-core/app/worker_activation_runtime_plan_review/store.py) | Core owns durable evidence and trusted eligibility. The existing reader reads v0.55, not a hypothetical v0.58 record. |
| [v0.56 closure tests](../../services/atlas-core/app/worker_activation_runtime_plan_review/test_release_closure.py) and [UI parser](../../services/mission-control/src/api/workerActivationRuntimePlanReview.ts) | Recursive lineage, permanent no-replay, strict authority, exact consumer isolation and read-only presentation are existing constraints. |
| [v0.57 proposal](worker-activation-runtime-interface-prerequisite-v1.md) | Its candidate inventory maps unresolved interfaces to owners and proof obligations; it explicitly requires a usefulness decision before implementation. |
| [Agent architecture](../../services/atlas-agent/ARCHITECTURE.md) and [execution-worker contract](../../services/atlas-execution-worker/README.md) | Repository execution and operational dispatch have separate registries, authentication, approvals and ledgers. Worker packaging does not implement installation runtime authority. |

## Likely next narrow boundary, conditional on v0.58

Working hypothesis: v0.58 might supply a closed, reference-only runtime interface
definition following the proposed v0.57 inventory. **This is an inference, not
an observed v0.58 decision.** If that hypothesis is confirmed, the likely v0.59
boundary is **Core-owned runtime interface definition review evidence** over
one exact owned v0.58 record/status pair.

The proposed incremental evidence is a deterministic correspondence check:
each frozen interface obligation has exactly one declared owning subsystem,
a bounded abstract input/output contract, explicit authority limits and a
fail-closed ambiguity/recovery rule. Findings would distinguish structural
coverage from unresolved proof obligations. They must not attest operational
correctness, successful contact, tested runtime compatibility or readiness.
Only closed literals and abstract references would be accepted, not executable
protocols, free-form specifications, endpoints, secrets or payloads.

A tentative marker is `worker_activation_runtime_interface_definition_review_recorded`.
It means a bounded review was recorded, never interface approved, prerequisite
satisfied, runtime defined operationally or worker-start admitted. Exact model,
marker, finding and refusal names remain unfrozen; P0 creates none of them.
The actual definition vocabulary and deterministic checks cannot be frozen
until v0.58 supplies a concrete contract.

| Actual predecessor outcome | Required v0.59 decision |
| --- | --- |
| v0.58 supplies a distinct reference-only definition with reviewable obligations | Confirm usefulness and freeze only its bounded structural review. |
| v0.58 remains an inventory, duplicates v0.57, or already performs this review | Re-analyze or retain documentation only; do not manufacture another identical evidence stage. |
| v0.58 defines contact, effects, admission, or different authority | Re-analyze against that accepted contract; this proposal cannot authorize an adapter or inherit new effects. |
| v0.58 is absent, incomplete or lacks durable owned lineage | Keep P1-P5 blocked. Never skip to v0.56 or a synthetic v0.58 wrapper. |

## Predecessor requirements before normative selection

1. Record the accepted v0.57 and v0.58 contracts, exact integration commit(s),
   actual P1-P5 closure evidence and outstanding external gates. Reconcile
   concurrent changes explicitly. A release label or local tag is not record
   identity, production readiness or proof of publication.
2. Freeze the sole direct v0.58 record/status schemas and owned read interface.
   Resolve authenticated owner and candidate scope; pin exact predecessor ID,
   record fingerprint, paired status fingerprint and expiry. Reject caller
   supplied nested evidence, latest-record fallback, alternate predecessors,
   queue lookup and worker lookup. Do not mutate or consume predecessor journals.
3. Preserve the complete accepted v0.58 lineage, including v0.57 if required by
   its final contract. The observed tail is v0.56 review -> v0.55 plan -> v0.54
   runtime admission -> v0.53 runtime prerequisite -> v0.52 queue receipt ->
   v0.51 acknowledgement admission -> v0.50 acknowledgement prerequisite ->
   v0.49 claim admission -> inherited v0.48-v0.20 pairs. The edges above v0.56
   are unknown here. Preserve every historical owner, candidate, ID, timestamp,
   limit, abstract subject and versioned fingerprint without normalization or
   truncation. v0.52 shares `admission_id` with v0.51; v0.54 has a distinct
   `runtime_admission_id`. Do not invent a receipt ID.
4. Reparse strict closed predecessor models, including copied/constructed
   objects; validate their own hash/UUID domains and status derivation at the
   recorded evaluation time. Independently check current eligibility with
   trusted whole-second UTC Core time. Reject future, stale, expired, foreign,
   missing, ambiguous, corrupt, mismatched or altered-authority evidence.
   Preserve historical evaluation times. No freshness renewal; successor
   expiry must equal the pinned predecessor expiry within all inherited limits.
5. Confirm complete recursive evidence fits existing ceilings: 192 KiB per model,
   a 30-second freshness ceiling, 16 reservations per owner, 256 globally and
   256 MiB main database pages (not a filesystem quota). These are observed
   v0.56 constraints, not claims about final v0.58 constants. Reconcile stricter
   predecessor constraints and refuse overflow/expiry; widening limits to fit
   another stage requires a separate decision.
6. Demonstrate useful new information beyond predecessor findings, freeze
   closed review semantics, separate hash/UUID domains and vectors, permanent
   owner/candidate/exact-predecessor subject binding, and exact API/consumer
   scope. A normative selection must resolve all gates before P1 starts.

The [v0.52 receipt](controlled-worker-queue-claim-lease-acknowledgement-boundary-v1.md)
contains injected already-observed assertions. Its presence in lineage does not
prove that Core contacted or independently verified a queue.

## Authoritative subsystem and blocked downstream authority

Core installation evidence is the only proposed authoritative subsystem: it
would own review evaluation, fingerprints, eligibility and persistence.
Mission Control would project owned evidence without approving it. Agent and
worker authorities retain their separate responsibilities and gain no consumer.

The observed seven blockers remain the conservative planning ceiling:

```text
worker_activation_runtime_not_defined
store_contact_not_defined
runtime_contact_not_defined
worker_start_admission_not_defined
worker_start_not_defined
agent_invocation_not_defined
execution_start_boundary_not_defined
```

v0.59 review itself removes no blocker. If final v0.58 changes that inventory,
reconcile this proposal before selection rather than silently adding or deleting
blockers. Preserve the observed `evidence_only=true`, `reference_only=true`,
`payload_bytes=0`, all 67 false authority/material fields and historical true
evidence markers; enumerate the accepted predecessor's exact fields at freeze.

| Separate future authority | Still-required boundary |
| --- | --- |
| Core runtime definition and start admission | Accepted installation runtime identity, capability, lifecycle and separate exact admission decision; structural review is not approval. |
| Worker-owned store/runtime contact | Authenticated peer and subject, bounded allowed operations, effect limits and fail-closed uncertainty handling; a Core SQLite journal is not a worker contact interface. |
| Worker start | Separately authorized one-shot protocol with reservation before effects and durable no-replay. |
| Agent invocation | Independently accepted installation intent, authentication and exact approvals; no conversion into repository or operational registries. |
| Execution start | Exact execution request, capability checks, approvals and durable ledger; review is not a worker execution request. |

No queue claim/lease/ack/consume/requeue, store/runtime/worker contact, activation,
start admission/build/start, Agent invocation, execution authorization/start,
process/shell/container execution, scheduler/workflow/dispatch, provider or
repository or guest mutation, installation, deployment, rollback, publication,
retry/resend, repair or replay bypass follows from this proposal. Home Assistant
remains blocked without installation artifacts. No production wiring, enabling
setting, adapter, credential handling or compose change belongs to this P0.

## Expected file ownership and conditional P1-P5 responsibilities

Only this version-specific planning file is owned by the current card. Future
paths below are proposed, do not exist by virtue of P0, and require reconciliation
with accepted v0.58 naming. Historical source files remain owned by their
predecessor phases. Shared ROADMAP, release checklist, changelog, release tags
and deployment state are outside this card.

| Phase | Proposed ownership and exit evidence after normative selection |
| --- | --- |
| P1 | Core `services/atlas-core/app/worker_activation_runtime_interface_definition_review/contract.py` and adjacent contract tests: pure immutable closed review models, fixed correspondence findings, exact recursive v0.58 validation and separate identity vectors. Cover extras/duplicate keys, strict literals, copied models, wrong hashes/owner/subject, stale/future/expired evidence, unresolved obligations and bounds. No I/O or runtime imports. |
| P2 | Same new Core package: `readers.py`, `service.py`, `store.py` and durability tests. Default-off explicit construction, exact owned v0.58 reader, separate bounded append-only journal, atomic key and permanent subject reservation, predecessor/time revalidation under both write locks. Exact duplicates return history without renewed eligibility or predecessor reads. Test restart, multiprocess races, partial writes, audit/response loss, corruption and unchanged predecessor bytes; ambiguity is terminal, with no retry/repair/eviction or replacement through new keys. |
| P3 | Proposed `services/atlas-core/app/routes/worker_activation_runtime_interface_definition_review.py` and route tests. Freeze minimum candidate/owner-scoped evidence POST/list GET/item GET, dedicated evaluate/read permissions, Origin/CSRF, strict bounded JSON, idempotency, redaction and response rebinding. Missing/disabled service fails closed. Prove exact methods/OpenAPI and no production construction or runtime action endpoint. Shared composition changes need a separately scoped future card. |
| P4 | Proposed Mission Control `src/types/workerActivationRuntimeInterfaceDefinitionReview.ts`, matching `src/api/` reader and adjacent nested component/tests under `services/mission-control/`. GET-only display beneath the exact v0.58 predecessor if its presentation exists; otherwise defer rather than invent a bridge. Validate closed evidence, owner/IDs, immutable predecessor, findings, blockers and authority. Core owns hashes/time eligibility. Cover scope reset, late responses and loading/missing/unavailable/expired states; no create/start controls, polling, persistence or standalone navigation. Run UI tests/build/lint. |
| P5 | New Core package closure tests and version-specific validation evidence. Prove actual accepted v0.58-to-v0.59 durable lineage, strict authority, permanent no-replay, exact Core/API/UI consumers and zero Agent/worker consumers. Run relevant historical closure/isolation/Home Assistant gates and P1-P4 checks; review the full diff adversarially. Do not broaden allowlists with wildcard exclusions or weaken fingerprint import checks. Record real results and external limitations; shared release-state updates belong to separately authorized release work. |

No current production file, test allowlist, Agent/worker package, Provider Intent,
Discovery, installation artifact or deployment configuration is assigned to P0.
P1-P5 above are responsibilities, not claims of work performed or permission to
start while predecessor gates remain unresolved.

## P0 validation evidence

Validation is limited to this document's assumptions and isolation. Run the
existing v0.56 contract and release-closure tests against this checkout, validate
relative links and required planning coverage, check the complete diff for
whitespace and version-specific scope, and perform hostile review before a local
documentation commit. Results are recorded below after execution. These checks
cannot validate absent v0.58 code or establish final successor lineage.

Focused tests passed: existing Core v0.56 `test_contract.py` and
`test_release_closure.py`, run through the selected interpreter's `-m pytest`
with `PYTHONPATH=services/atlas-core`: **102 passed, 340 warnings in 151.89s**.
Warnings included Pydantic schema-shadowing and hostile-fixture serialization
warnings. Planning coverage and all 14 relative links passed local checks.

Hostile review passed: the complete document was checked for invented v0.58
lineage, premature phase authorization, redundant review semantics, authority
escalation, stale/ambiguous evidence acceptance, replay loopholes, speculative
path ownership and missing validation obligations. The candidate and its
fallbacks remain conditional; all predecessor confirmation gates remain open.
The final diff contains only this v0.59 planning file and passes whitespace
checks. No runtime behavior or shared release-state change is included.
