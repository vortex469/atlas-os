# v0.62 P5 — retained authority isolation; release closure open

Inspected baseline: `994d98af` (2026-09-19). The [normative v0.62
boundary](v0.62-authority-boundary.md) retains the Core-owned v0.57
prerequisite pair with the integrated v0.61 hardening. V0.62 P5 records
retained-boundary release evidence; it does not select a successor schema or
close the inherited release obligations.

## Closure regressions

`test_v062_release_closure.py` adds 43 cases. It rejects inventory-admission,
runtime-definition and definition-review marker forms across all shared
Core/UI evidence envelopes, including both true and false values. It also
forbids successor and operational consumers across Core, Agent, execution
worker, Mission Control, configuration, deployment, scripts and workflows.
Test sources remain evidence and are excluded from the production scan.

The retained v0.62 suites continue to prove the exact schemas, complete
lineage, bounds, bounded journal restart/no-replay behavior and journal
application-ID refusal. Existing closure tests preserve the exact twelve
production consumers, seven blockers, 67 fixed-false authority fields and the
GET-only non-authoritative Mission Control projection.

## Reproducible focused validation

Run from the managed worktree with the selected interpreter:

```bash
mkdir -p .task-evidence/v062-p5
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v062_release_closure.py \
  --basetemp=.task-evidence/v062-p5/pytest-new --tb=short --disable-warnings
```

Focused tests passed: **43 passed**, exit 0 in 73.71 seconds. The new test
file is the only production-tree addition; no runtime, dependency, deployment
or historical isolation implementation is changed.

## Release disposition and hostile review

**V0.62 release closure remains open.** Repository-integrated predecessor
acceptance, full Core Ruff/validation and unrestricted worker API validation
remain required. The retained v0.61 Mission Control results and this focused
P5 suite are evidence, not a release, tag, publication, deployment or runtime
enablement.

Hostile review passed: the closure suite was reviewed for false-value bypasses,
deep shared-envelope coverage, scanner exclusions, successor naming variants,
operational consumers and accidental authority promotion. Documentation was
checked for synchronized v0.62 references, preserved open gates and no new
runtime claims. `git diff --check` passed.
