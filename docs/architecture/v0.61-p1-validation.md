# v0.61 P1 request — retained immutable Core contract

The [normative v0.61 freeze](v0.61-authority-boundary.md) incorporates the
v0.60 contract and retains the existing v0.57 prerequisite record/status pair.
That production implementation already exists; no production correction was
needed. Successor P1-P5 remain deferred pending the freeze's reopening gates.
This regression change does not select a successor or close release acceptance.

`test_v061_retained_boundary.py` adds 17 cases for the retained boundary:

- Both complete golden pair members validate before hostile mutation.
- Invented inventory admission, definition and definition-review claims are
  rejected with either true or false values, even with recomputed fingerprints
  and model-copy bypasses. Errors must identify the extra field itself.
- Recomputed fingerprints cannot authorize a v0.61 schema substitution.
- Pair inventories, nested entries and fingerprint values remain immutable.
- The freeze retains the exact schema names, 192 KiB and 30-second limits.

## Validation

Run from the managed worktree using the selected interpreter and
`PYTHONPATH=services/atlas-core`, with `-m pytest -xq --tb=short
--disable-warnings`, against these files under
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`:

- `test_v061_retained_boundary.py`
- `test_v060_retained_boundary.py`
- `test_contract.py`

Use `--basetemp=.task-evidence/v061-p1/pytest-tmp` after creating its parent.
Local output is retained in `.task-evidence/v061-p1/pytest.log`. The new suite
is also rerun after the fixture lint correction, with output in
`.task-evidence/v061-p1/final-pytest.log`. Focused Ruff checks cover the new test.
Full service, API, UI and release gates are outside this retained P1 regression.

Focused tests passed: **134 passed, 326 warnings in 408.48s**, exit 0.
The final fixture rerun passed **17 tests, 326 warnings in 22.57s**, exit 0.
Ruff lint and formatting checks passed for the new test file.

Hostile review passed: reviewed the complete two-file diff after test success
for false-positive rejection, shared fixture mutation, model-copy bypasses,
rehashed authority/schema substitution, nested immutability and scope expansion.
The positive controls accept full golden evidence; negative authority cases
require the exact extra-field error, and fixture dumps stay unchanged.
Existing contract tests supply independent hash/UUID vectors, strict authority,
recursive lineage, expiry and envelope-bound coverage. No runtime, API, consumer
allowance or dependency changes were introduced. `git diff --cached --check`
passed. These results validate retention, not a deferred successor contract.
