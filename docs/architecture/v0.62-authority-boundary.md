# v0.62 Sync — retained authority boundary

Status: **Normative repository planning freeze. Retain the Core-owned v0.57
prerequisite record/status pair with repository-integrated v0.61 hardening.
Successor P1-P5 remain deferred. No successor implementation is authorized.**

## Integrated baseline and decision

Inspected on 2026-09-19 at `984c134b6bcfd203347656a74a564c2a66bca637`, which
integrates v0.61 P5. Its v0.62 P0 predecessor and the v0.61 Sync
`217e1aa9c3a49b8a82d9462ecb099c7442d168fa` are verified ancestors. The
[v0.61 P5 evidence](v0.61-p5-validation.md) records the retained P1-P5
changes and current release obligations. Source integration does not establish
release acceptance.

This Sync supersedes the provisional P0 selection and conditional phase
proposals with documentation-only retention. The [v0.61 normative boundary](v0.61-authority-boundary.md),
including its incorporated v0.60 technical contract, remains binding in full:
exact schemas, identities, fingerprint and UUID domains, recursive lineage,
time, durability, API, ordered inventory, authority ceiling and consumer
ownership are unchanged. The v0.62 label creates no evidence stage, migration,
schema, marker, domain, reader or route.

| P0 candidate | Frozen disposition |
| --- | --- |
| Inventory admission or another consistency review | Deferred: correspondence is already checked and no distinct consumer decision is demonstrated. |
| Runtime identity/capability composition/lifecycle definition | Deferred: trusted provenance, distinct usefulness and bounded embedding remain unproved. |
| Runtime definition review | Deferred: no accepted definition pair exists to review. |
| Store/runtime contact or adapter | Requires a separate authority design for authenticated subjects, operations, effects and recovery. |
| Worker start, Agent invocation or execution integration | Blocked by unresolved runtime/contact proofs and missing exact approval and durable-effect boundaries. |

## Retained output and authority ceiling

The accepted pair remains `WorkerActivationRuntimeInterfacePrerequisiteV1` /
`WorkerActivationRuntimeInterfacePrerequisiteStatusV1`, with schemas
`worker-activation-runtime-interface-prerequisite-v1` and
`worker-activation-runtime-interface-prerequisite-status-v1`. It retains the
complete v0.56 review and inherited v0.55-v0.20 lineage. All six
predecessor/stage UUID fields, owner/candidate binding, version-specific hashes,
192 KiB model/response bound, 30-second freshness, 16 per-owner reservations,
256 global reservations and 256 MiB main SQLite page bound remain exact.

The v0.61 integrated delta is retention hardening only: immutable contract and
shared-envelope refusal, bounded durable journal/no-replay behavior, guarded
create-failure isolation, nested non-authoritative Mission Control evidence,
and release/isolation regressions. It adds no successor pair, reader, route,
consumer or authority field.

All seven ordered blockers remain unresolved. Preserve `evidence_only=true`,
`reference_only=true`, `payload_bytes=0`, all 67 fixed-false authority/material
fields and the original meanings of historical true evidence markers. No queue
claim/lease/ack/consume/requeue, contact, activation, start, Agent invocation,
execution, installation, deployment, rollback, publication, retry/resend or
repair/replay authority follows. Mission Control remains a nested,
non-authoritative GET-only projection; Agent, worker and operational consumers
remain zero.

## Phase disposition and reopening

| Phase | Frozen v0.62 disposition |
| --- | --- |
| P1 | Successor contract/evaluator/vectors deferred; retain the existing Core contract. |
| P2 | Successor reader/service/journal deferred; retain the existing owned reader and journal. |
| P3 | Successor API deferred; retain the existing guarded prerequisite API. |
| P4 | Successor presentation deferred; retain nested GET-only evidence. |
| P5 | Successor release closure deferred; retained-boundary isolation evidence is recorded in [v0.62 P5](v0.62-p5-validation.md), while predecessor acceptance remains independent. |

Reopening requires a separate normative decision satisfying all six gates in
[v0.62 P0](v0.62-provisional-authority-boundary.md#evidence-required-before-selection):
accepted predecessor, distinct trusted predicate, exact owned pins, recursive
integrity and measured bounds, durable uncertainty/no-replay, and hostile
no-authority-promotion evidence. If any gate remains unproved, retention
continues. Release acceptance alone cannot select a successor.

## Release and synchronization scope

Repository-integrated v0.61 and inherited release acceptance remain open where
recorded in the [release checklist](../RELEASE_CHECKLIST.md): predecessor
acceptance, full Core Ruff/validation and unrestricted worker API validation
remain separate obligations. The v0.62 P5 regression is isolation evidence,
not permission to close those obligations. The current full Mission Control
pass recorded by v0.61 P5 is retained evidence, not a new v0.62 release gate.

Sync owns this document, its [validation record](v0.62-sync-validation.md), the
P0 supersession notice, `ROADMAP.md` and `docs/RELEASE_CHECKLIST.md`.
Production code, tests, dependencies, scanner allowances and deployment remain
outside this documentation freeze. No release, tag, publication or runtime
enablement follows.
