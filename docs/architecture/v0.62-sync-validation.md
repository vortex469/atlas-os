# v0.62 Sync validation — integrated v0.61 retention

Inspected baseline: `984c134b6bcfd203347656a74a564c2a66bca637` (2026-09-19).
The [normative freeze](v0.62-authority-boundary.md) reconciles v0.62 P0 with
integrated v0.61 P5 and retains the v0.57 pair. Successor P1-P5 remain
deferred; no release acceptance follows.

## Focused validation

Run from the managed worktree with the selected interpreter's `-m pytest -q`,
`PYTHONPATH=services/atlas-core`, and
`--basetemp=.task-evidence/v062-sync/pytest --tb=short --disable-warnings`.
The focused selection is the v0.61 retained closure, boundary, exact consumer,
authority-ceiling and contract tests under
`services/atlas-core/app/worker_activation_runtime_interface_prerequisite/`.
These verify retained schemas and limits, recursive successor-marker refusal,
the exact twelve production consumers, 67 false fields and zero operational
consumers. Documentation checks additionally verify local links, the three
baseline ancestors and whitespace.

The Sync changes are documentation-only. No production or test source is
changed to bypass a failure, and full release gates are not rerun here.

## Results and hostile review

Focused tests passed: **47 passed**, exit 0. The reproduction command and
worktree-local evidence location are recorded above; the temporary basetemp
parent must exist before invocation.

Documentation checks passed: local links, baseline ancestry references and
new-file whitespace checks pass. Historical release obligations and the known
open gates remain explicitly preserved.

Hostile review passed: inspected the complete five-file Sync change against the
v0.61 normative boundary, P0 analysis, integrated P5 evidence and retained Core
contract. Checked invented schemas or IDs, duplicate evidence stages, trusted
provenance claims, unstable status pins, widened bounds, consumer exceptions,
authority promotion and premature release closure. No runtime behavior or
successor authority is introduced.
