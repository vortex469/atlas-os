# v0.59 Sync validation — retained repository boundary

Baseline: `607fbc48f8202d371294012ac1ad6424ec37c42b`, integrating v0.59 P0
`5a7bf10cd5ac17ea7d7cd02210aea7300cc435fa` and v0.58 P5
`913bbc86cdcb59667c435d404537ea200df1b634`. The
[normative v0.59 contract](v0.59-runtime-interface-boundary.md) selects the retained
Core-owned v0.57 prerequisite inventory with integrated v0.58 hardening.
Definition-review P1-P5 are deferred because the assumed definition is absent
and inventory correspondence already belongs to the retained validators.

## Focused validation

Run from the managed worktree using the selected interpreter:

```bash
mkdir -p .task-evidence/v059-sync
PYTHONPATH=services/atlas-core /opt/atlas/.venv/bin/python -m pytest -q \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v058_release_closure.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py \
  services/atlas-core/app/routes/test_worker_activation_runtime_interface_prerequisite.py \
  -k 'v058 or exact_v057_production_consumers or normative_inventory_authority or frozen_versioned_hash or normative_false_inventory or permissions_openapi_method or exact_registered_surface or production_startup' \
  --basetemp=.task-evidence/v059-sync/pytest-tmp --tb=short --disable-warnings
```

Focused tests passed: **19 passed, 136 deselected, 327 warnings in 38.21s**,
exit 0. These verify retained inventory/bounds, hash/UUID vectors, all 67 false
fields, exact production consumers and API/OpenAPI permissions/methods, no
production construction, and rejection of invented admission authority in all
five Core/shared-UI-golden envelopes. Warnings include inherited Pydantic
schema-shadowing warnings. The initial invocation errored during setup because
the worktree-local basetemp parent did not exist (19 setup errors); creating that
parent and rerunning the identical selection produced the result above.

The ignored local log is `.task-evidence/v059-sync/focused.log`; this committed
document is the portable result. No full Core/API, UI, durable service/store,
browser or repository suite run is claimed for this documentation-only Sync.
Existing v0.58 P5 evidence remains the source of those historical results.

## Documentation and hostile review

Local checks compare the new normative table directly with Core's seven ordered
inventory triples, both schema literals and all six ID fields. Git ancestry
checks confirm the two source dependencies and v0.58 implementation parent.
Relative link targets are checked inside the managed worktree. P0 text after
its new supersession notice and historical shared release evidence remain
byte-exact. The working-tree scope is exactly five Markdown files: the normative
contract, this validation record, the P0 notice, ROADMAP.md and the release
checklist. No production, test, allowlist, dependency or deployment file changes.

Hostile review passed: reviewed the full five-file change after focused tests.
Checked for fabricated v0.58 definition schemas or IDs, duplicate-stage
justification, premature phase authorization, mutable GET status used as a
stable predecessor pin, truncated lineage, invented receipt identity, replay or
expiry renewal, changed false authority, widened bounds and consumer allowances,
and release acceptance inferred from integration. The freeze explicitly defers
the unsupported review, preserves complete retained semantics and keeps all
seven proof obligations unresolved. No remaining defect was found in this scope.
`git diff --check` passes. Staging is blocked as recorded below; no staged
validation pass is claimed.

## Release and commit disposition

[V0.58 P5 evidence](v0.58-p5-validation.md) records open full Mission Control
test/build/lint gates due to unavailable locked dependencies, and an observed
worker SQLite initialization race followed by a passing confirmation. Neither
is repaired or waived here; historical P4 success is not a full P5 UI pass.
Repository planning can freeze the integrated Core/API boundary while
v0.57/v0.58 release closure remains open. No release tag, publication, deployment,
runtime implementation or enablement is claimed.

The required local commit could not be created. `git add` failed because the
managed worktree's Git index lock is on a read-only filesystem. The execution
policy does not permit escalation. No alternate Git metadata, relocation or
permission bypass was attempted. All five documentation changes remain in the
managed worktree; the task remains incomplete until normal staging and commit
are available. Intended commit title:
`docs: freeze v0.59 against integrated v0.58 Core/API boundary`.
