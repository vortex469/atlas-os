# v0.60 P1 request — retained Core boundary validation

The [synchronized boundary](v0.60-runtime-definition-boundary.md) selects retention
of v0.57 Core models/evaluation with integrated v0.59 hardening and explicitly
defers runtime definition P1-P5. The requested exact-boundary implementation is
already present. No new production implementation is necessary or selected.
In particular, v0.59 is an integrated code boundary, not a new evidence pair;
the exact predecessor remains the complete v0.56 review/status pair.

The task adds `test_v060_retained_boundary.py` beside the retained Core contract.
Its 13 cases check the normative inventory/schema mapping, refusal of definition
claims (including false values) injected through model-copy bypasses at five
input levels, and rejection of successor record/status schemas even after
recomputing their fingerprints. The fixture first proves the complete shared
Core/UI golden evidence is accepted. Refusals must remain redacted, carry no
recognized review, inventory or expiry, and retain a valid evaluation hash;
the record builder must also reject the forged inputs.

## Validation command

From the managed task worktree, with the selected interpreter and
`PYTHONPATH=services/atlas-core`, run `-m pytest -xq` against:

- `services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v060_retained_boundary.py`
- `services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py`
- `services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_release_closure.py`
- `services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v059_release_closure.py`

Use `--basetemp=.task-evidence/v060-p1/pytest-tmp --tb=short --disable-warnings`
after creating `.task-evidence/v060-p1`. The retained suites cover closed immutable
models, independent hash/UUID vectors, exact recursive lineage, foreign/stale
evidence, reservation replay, durable restart, all 67 false authority fields and
the exact production consumer set. Full API/UI/repository release gates are
outside this regression-only change and are not claimed closed.

Focused tests passed: **150 passed, 350 warnings in 505.38s**, exit 0, using
the selected interpreter. Execution output is retained locally in
`.task-evidence/v060-p1/pytest.log`. Initial test development exposed a missing
temporary-directory parent and two assertion assumptions (documentation line
wrapping and the recursive strict-literal error shape); these were corrected
before the complete passing run. No production correction was needed.

Hostile review passed: after the passing run, reviewed the complete three-file
diff for scope expansion, false-positive refusal tests, fixture contamination,
model-copy bypasses, rehashed schema substitution and misleading release claims.
The positive fixture establishes valid evidence before mutation; schema refusals
must come from strict literal validation, not merely a mismatched outer pin.
Existing contract and closure tests cover foreign/stale evidence, replay,
independent fingerprint vectors, authority coercion and production consumers.
No production/API/UI/runtime files or historical scanner allowances changed.
`git diff --cached --check` passed.
