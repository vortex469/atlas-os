# v0.62 P1 — retained immutable Core contract

The v0.62 normative freeze retains the existing Core-owned v0.57 prerequisite
record/status pair. It does not select a successor schema or evaluator:
successor P1–P5 remain deferred because trusted provenance, a distinct
predicate and bounded feasibility are unproved.

`test_v062_retained_boundary.py` locks the retained contract against the
failure modes relevant to this phase:

- both complete recursive pair members remain valid;
- the exact schemas, inventory and model bounds remain unchanged;
- invented inventory-admission, runtime-definition and definition-review
  markers are rejected with or without recomputed fingerprints;
- rehashed v0.62 schema substitutions are rejected; and
- nested evidence and fingerprints remain deeply immutable.

No runtime, persistence, API, UI, consumer or authority field is added.

## Validation

Run from the managed worktree with `PYTHONPATH=services/atlas-core`:

```text
/opt/atlas/.venv/bin/python -m pytest \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v062_retained_boundary.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_v061_retained_boundary.py \
  services/atlas-core/app/worker_activation_runtime_interface_prerequisite/test_contract.py \
  --tb=short --disable-warnings
```

This is retained-boundary evidence, not successor release acceptance.
