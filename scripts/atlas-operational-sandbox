#!/usr/bin/env bash

set -euo pipefail

atlas_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$atlas_root/services/atlas-core"
exec python -m app.operational_dispatch.sandbox "$@"
