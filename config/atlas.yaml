atlas:
  name: Atlas OS
  release: Foundry
  assistant: Orion
  host: 0.0.0.0
  port: 8643

infrastructure:
  domain: home.arpa

proxmox:
  host: 10.10.60.100
  port: 8006
  node: vorex469
  verify_ssl: false

home_assistant:
  url: http://10.10.20.100:8123

docker:
  socket: unix:///var/run/docker.sock

inventory:
  file: /opt/atlas/inventory/services.yaml

audit:
  database: /opt/atlas/data/action_history.db
  max_entries: 5000
  retention_days: 90

intelligence:
  provider_timeout_seconds: 10
  telemetry_database: /opt/atlas/data/provider_intelligence.db
  telemetry_max_entries: 10000
  telemetry_retention_days: 30
